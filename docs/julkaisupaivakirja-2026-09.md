# Julkaisupäiväkirja 14.8.–3.9.2026

Tähän tiedostoon kirjataan päiväkohtaisten työpakettien tila. Salaisuuksia, tunnuksia, henkilötietoja tai yksityisten palautteiden sisältöä ei kirjata.

## Vastuuhenkilöt ja käyttöoikeudet

- Kaikkien REL-, TEST-P2- ja CONTENT-P1-pakettien vastuuhenkilö on Eero Tuomenoksa ja varahenkilö Nina Ziessler. Taulukon omistajasarake kuvaa lisäksi paketissa tarvittavat toteutus- ja hyväksyntäroolit.
- Tekninen vastuuhenkilö on toistaiseksi Eero Tuomenoksa.
- Eero Tuomenoksalla on tarvittavat Cloudcity-, WordPress-, Firebase- ja DNS-oikeudet.
- Osa Google- ja Firebase-toiminnoista käyttää SeniorSurf-tiimin hallinnoimaa yhteiskäyttöistä Google-roolitiliä. Tilin tunnuksia tai tarkkaa osoitetta ei kirjata tähän tiedostoon.
- WordPressin tekninen konsultointiyhteys on Fakiirimediassa ja palvelinympäristön tukiyhteys Cloudcityn asiakaspalvelussa. Yhteystiedot säilytetään repositorion ulkopuolella.
- Staging-muutosikkunan päiväksi on hyväksytty tiistai 25.8.2026 ja tuotantojulkaisun päiväksi tiistai 1.9.2026. Tarkat kellonajat vahvistetaan ennen muutoksia.
- Kiireellisen palautuksen ensisijainen tekninen yhteyshenkilö on Eero Tuomenoksa; WordPress-vaikutuksissa konsultoidaan Fakiirimediaa ja palvelinympäristön häiriöissä Cloudcityn asiakaspalvelua.

## Tilannekuva

| Paketti | Päivä | Omistajarooli | Tila | Riippuvuudet hyväksytty | Tarkistaja | Todiste / huomio |
| --- | --- | --- | --- | --- | --- | --- |
| REL-00 | pe 14.8. | SeniorSurf-tuotevastuu | valmis | kyllä |  | Vastuuhenkilö, varahenkilö, oikeuksien haltija, tekniset yhteydet ja muutosikkunoiden päivät nimetty; Fakiirimedian vastaus vastaanotettu 17.8.2026 klo 11.42. |
| REL-01 | ma 17.8. | Fakiirimedia + tekninen ylläpito | valmis | kyllä | Nina Ziessler | Nina hyväksyi rakenteen, tulokset ja väliaikaisen SSH-poikkeaman 17.8.2026 klo 14.45. |
| REL-02 | ti 18.8. | kehitys | valmis | kyllä | Nina Ziessler | Stagingin 15 taulun alkuskeema, UTC-istunto, varmuuskopio/palautus ja versionoitu päivitysmigraatio testattu onnistuneesti; tuotantotietokantaa ei ole vielä luotu tai muutettu. |
| REL-03 | ke 19.8. | kehitys | valmis | kyllä |  | WordPressistä riippumaton PHP 8.4 -API-perusta, health-reitti ja turvalliset yhteiskomponentit toteutettu; 14 sopimustestiä, paikallinen HTTP-koe, salaisuusskannaus ja tuotantokäännös läpäisty. |
| REL-04 | to 20.8. | kehitys | valmis | kyllä |  | Kolme rajattua julkista listareittiä ja neljä validoitua kirjoitusreittiä toteutettu; 23 sopimustestiä ja paikallinen HTTP-koe läpäisty ilman Cloudcity- tai WordPress-muutoksia. |
| REL-05 | pe 21.8. | kehitys + tuotevastuu | valmis | kyllä |  | Firebase-tunnistus toimii stagingissä kahdella hyväksytyllä Google-ylläpitäjällä, aktiivisuuden poisto/palautus on testattu ja PHP 8.4.24 -lint sekä 33/33 sopimustestiä läpäisivät. Ylimääräiset password-provider-käyttäjät on rajattu pois ja disabloitu. Uusien TEST-P2-tulosten puuttuessa tuotevastuu siirsi niiden lukituksen REL-10:een. |
| REL-06 | ma 24.8. | kehitys | valmis | kyllä |  | Provider-siirtymä, ympäristöbuildit, Cloudcity-integraatio ja Firebase-palautusasetus ovat teknisesti valmiit. Nykyinen staging-build läpäisi TypeScript-, build- ja salaisuustarkistukset; REL-05 hyväksyttiin ja TEST-P2 siirrettiin tuotevastuupäätöksellä REL-10:een. |
| REL-07 | ti 25.8. | Fakiirimedia + ylläpito + kehitys | valmis | kyllä |  | Cloudcity-stagingin nykyinen frontend ja PHP-API on synkronoitu ja uusintatestattu ennakkoon 24.8.; julkiset sivut, oma 404, tietokanta-health, molemmat ylläpitäjät, suojausotsikot, välimuistit, pakkaus ja WordPressin jälkeen-savukoe toimivat. Palvelimelta ei löytynyt poistettavaa ZIPiä. |
| REL-08 | ke 26.8. | kehitys + tietosuojavastuu | valmis ennakkoon | kyllä |  | Täysi Firestore-vienti, idempotentit täys- ja delta-tuonnit, rivimäärä-, aikaväli- ja tiivistetäsmäytykset sekä yksityisyyskokeet valmistuivat 24.–25.8. Väliaikainen Admin SDK -avain mitätöitiin ja paikallinen avaintiedosto poistettiin. |
| REL-09 | to 27.8. | kehitys + Fakiirimedia | valmis ennakkoon | kyllä |  | Tausta-ajot, staging-cron, käyttötilasto, erillinen tietokantapalautus, luku-/kirjoituskoe, frontend-palautusharjoitus, WordPress-savukoe sekä CONTENT-P1-selosteet ja säilytysajat ovat valmiit. |
| REL-10 | pe 28.8. | kehitys + tuotevastuu | käynnissä ennakkoon | kyllä |  | Julkisen navigaation sisäiset linkit ja beta-merkintä on poistettu. Kolme uutta TEST-P2-havaintoa on korjattu. Nimipäivä- ja AI-toiminnot eivät näy tai lataudu, ja paikallisuutisten oletuspiilotus sekä asetuksesta avaaminen on selainvarmistettu. |
| REL-11 | ma 31.8. | testaajat + kehitys + tuotevastuu + Fakiirimedia | odottaa | ei |  |  |
| REL-12 | ti 1.9. | julkaisija + Fakiirimedia + viestintä | odottaa | ei |  |  |
| REL-13 | ke 2.9. | kehitys + testaaja | varapäivä | ei |  |  |
| REL-14 | to 3.9. | tuotevastuu + julkaisija | odottaa | ei |  |  |
| TEST-P2 | ma–pe 17.–21.8. | SeniorSurf-testausvastuu | siirretty REL-10:een | kyllä |  | Uusia testaustuloksia ei ollut saapunut 24.8.2026; tuotevastuu siirsi koonnin ja P1/P2-lukituksen REL-10-julkaisuporttiin. |
| CONTENT-P1 | ma–to 17.–27.8., vain arkipäivinä | sisältö- ja vastuuroolit | odottaa | ei |  |  |

## Pakettimerkinnän malli

### Paketti: REL-XX

- **Päivämäärä:**
- **Omistaja:**
- **Tarkistaja:**
- **Riippuvuudet tarkistettu:**
- **Muutokset ja päätökset:**
- **Muokatut tiedostot tai palvelinasetukset:**
- **Suoritetut komennot ja testit:**
- **Tulokset ja todisteiden sijainti:**
- **WordPress-savukoe ennen/jälkeen:**
- **Tietokanta- ja varmistusvaikutus:**
- **Avoimet P1/P2/P3-havainnot:**
- **Palautusohje:**
- **Seuraavalle paketille luovutettu:**
- **Tila:** valmis / estynyt

## Paketti: REL-00

- **Päivämäärä:** 14.–17.8.2026
- **Vastuuhenkilö:** Eero Tuomenoksa
- **Varahenkilö:** Nina Ziessler
- **Tekninen yhteyshenkilö:** Eero Tuomenoksa
- **Riippuvuudet tarkistettu:** kyllä
- **Muutokset ja päätökset:** kaikkien pakettien vastuuhenkilö- ja varahenkilömalli, käyttöoikeuksien haltija, tekniset konsultointiyhteydet sekä staging- ja tuotantopäivät on hyväksytty.
- **Muokatut tiedostot tai palvelinasetukset:** julkaisupäiväkirjan vastuuhenkilö- ja oikeusmerkinnät; ei REL-00:n palvelinasetusmuutoksia.
- **Suoritetut komennot ja testit:** ei teknisiä testejä REL-00:ssa.
- **Tulokset ja todisteiden sijainti:** Fakiirimedian vastaus arkkitehtuuri- ja PHP-moduulikysymyksiin vastaanotettiin 17.8.2026 klo 11.42; vastaus säilytetään repositorion ulkopuolisessa sähköpostiketjussa.
- **WordPress-savukoe ennen/jälkeen:** ei sovellu REL-00:n yhteydenottoon; myöhempi PHP-moduulimuutos ja sen savukoe kirjataan REL-01:een.
- **Tietokanta- ja varmistusvaikutus:** ei REL-00:ssa.
- **Avoimet P1/P2/P3-havainnot:** ei avoimia REL-00-havaintoja; muutosikkunoiden tarkat kellonajat vahvistetaan ennen kyseisiä muutoksia.
- **Palautusohje:** ei REL-00:n teknistä muutosta.
- **Seuraavalle paketille luovutettu:** REL-01:lle 17.8.2026.
- **Tila:** valmis

## Paketti: REL-01

- **Päivämäärä:** 17.8.2026
- **Vastuuhenkilö:** Eero Tuomenoksa
- **Varahenkilö:** Nina Ziessler
- **Tarkistaja:** Nina Ziessler
- **Tarkistajan hyväksyntä:** Nina Ziessler hyväksyi REL-01:n rakenteen, tulokset ja väliaikaisen yhteisen SSH-tunnuksen P2-poikkeaman 17.8.2026 klo 14.45 (EEST).
- **Tekninen yhteyshenkilö:** Eero Tuomenoksa
- **Riippuvuudet tarkistettu:** kyllä; REL-00 on valmis ja Fakiirimedian tekninen yhteyshenkilö on tavoitettu.
- **Muutokset ja päätökset:** tuotanto toteutetaan osoitteessa `aloitussivu.seniorsurf.fi` ja staging osoitteessa `staging.aloitussivu.seniorsurf.fi`. Kumpikin ympäristö erotetaan WordPressistä omalla hakemistollaan. Staging suojataan HTTP Basic Auth -käyttäjätunnuksella ja salasanalla.
- **Hyväksytty hakemisto- ja tallennusrajaus:**
  - tuotannon document root: `/home/seniorsurffi/aloitussivu.seniorsurf.fi/public_html`;
  - stagingin document root: `/home/seniorsurffi/staging.aloitussivu.seniorsurf.fi/public_html`;
  - kummankin ympäristön `secrets`, `logs` ja `protected_uploads` sijoitetaan ympäristökohtaiseen hakemistoon `public_html`-hakemiston ulkopuolelle;
  - WordPress säilyy erillisessä `/home/seniorsurffi/website.wp33403`-hakemistossa eikä Aloitussivu käytä sen sovelluskoodia tai tietokantaa.
  - WordPressin fyysinen juuri `/home/seniorsurffi/website.wp33403` on varmennettu SSH:ssa etsimällä `wp-config.php`-tiedoston sijainti ilman tiedoston sisällön lukemista;
  - WordPressin pääjuuren `.htaccess` on olemassa ja sisältää LiteSpeed-merkintöjä. LiteSpeed Cache vaikuttaa vain WordPress-asennukseen eikä Aloitussivun alidomaineihin tai API:in. Aloitussivun alidomain-ratkaisu ei edellytä tämän tiedoston muuttamista;
  - aktiiviset WordPress-lisäosat 17.8.2026: `acf-content-analysis-for-yoast-seo`, `post-2-post-for-acf`, `advanced-custom-fields-pro`, `akismet`, `all-in-one-wp-migration`, `all-in-one-wp-migration-unlimited-extension`, `broken-link-checker`, `wp-piwik`, `enhanced-media-library-pro`, `litespeed-cache`, `pages-with-category-and-tag`, `redirection`, `show-modified-date-in-admin-lists`, `wp-consent-api` ja `wordpress-seo`;
  - REL-01:n kannalta keskeiset lisäosat ovat Redirection ja All-in-One WP Migration. LiteSpeed Cache on rajattu WordPress-asennukseen eikä vaadi Aloitussivulle poikkeussääntöä. Aktiivisten lisäosien luettelossa ei näy erillistä Wordfence- tai vastaavaa tietoturvalisäosaa;
- **Hakemisto-, reititys- ja tietokantaeristys:**

  ```text
  seniorsurf.fi (WordPress)
    -> /home/seniorsurffi/website.wp33403
    -> WordPress-tietokanta
    -> /aloitus/ ohjataan myöhemmin Redirection-lisäosalla tuotantoon

  aloitussivu.seniorsurf.fi
    -> /home/seniorsurffi/aloitussivu.seniorsurf.fi/public_html
    -> saman originin /api/v1/
    -> Aloitussivun tuotantotietokanta ja rajattu tuotantokäyttäjä
    -> secrets, logs ja protected_uploads public_html-hakemiston ulkopuolella

  staging.aloitussivu.seniorsurf.fi (HTTP Basic Auth)
    -> /home/seniorsurffi/staging.aloitussivu.seniorsurf.fi/public_html
    -> saman originin /api/v1/
    -> Aloitussivun staging-tietokanta ja rajattu staging-käyttäjä
    -> secrets, logs ja protected_uploads public_html-hakemiston ulkopuolella
  ```
- **Ympäristömatriisi:**
  - PHP CLI: `8.4.23`, polku `/opt/alt/php84/usr/bin/php`;
  - PHP-laajennukset: JSON, cURL, PDO, PDO MySQL ja OpenSSL käytettävissä; PDO-ajurina `mysql`;
  - Composer: `2.9.3`;
  - cron-komento: käytettävissä polussa `/usr/bin/crontab`;
  - palvelimen nykyinen aikavyöhyke: `EEST`, UTC+03:00 (17.8.2026);
  - SSH: kirjautuminen vahvistettu;
  - SFTP: kirjautuminen vahvistettu, aloitushakemisto `/home/seniorsurffi`;
  - MariaDB-palvelin: `12.1.2-MariaDB-log`;
  - CloudCity Pro -paketin tietokantakapasiteetti: enintään 10 tietokantaa;
  - nykytila: yksi WordPressin käyttämä tietokanta, jossa on 93 taulua;
  - vapaata kapasiteettia: yhdeksän tietokantaa, joten Aloitussivulle voidaan varata erilliset staging- ja tuotantotietokannat;
  - REL-02:n tarvitsemat palvelin- ja käyttöoikeustiedot on varmennettu.
- **Loki- ja käyttöoikeusmalli:** stagingin ja tuotannon sovelluslokit pidetään erillisissä `logs`-hakemistoissa `public_html`-hakemistojen ulkopuolella. Lokit kierrätetään päivittäin ja säilytetään 30 päivää. Lokeihin ei tallenneta salasanoja, tunnuksia, palautetekstejä, liitteitä tai muita henkilötietoja. Tavoiteoikeudet ovat hakemistoille `750` ja lokitiedostoille `640`. Käyttöönoton tässä vaiheessa Eero ja varahenkilö voivat käyttää yhteistä SSH-tunnusta; tunnusta tai salasanaa ei säilytetä repositoriossa tai julkaisupäiväkirjassa. Henkilökohtaisiin tunnuksiin siirtyminen arvioidaan ennen tuotantojulkaisua.
- **Muokatut tiedostot tai palvelinasetukset:** PHP:n `nd_pdo_mysql`-moduuli aktivoitiin CloudCityssä. Suunnitelmat päivitettiin tiedostoihin `docs/cloudcity-tietokanta-p1-suunnitelma-2026-08-14.md`, `docs/julkaisun-paivakohtaiset-tyopaketit-2026-08-14.md` ja tähän julkaisupäiväkirjaan. Alidomaineja, DNS-tietueita, palvelinhakemistoja, tietokantoja tai Basic Auth -suojausta ei vielä luotu. Tunnukset säilytetään repositorion ja julkisen web-juuren ulkopuolella.
- **Suoritetut komennot ja testit:** PHP- ja PDO-laajennustarkistus, PDO-ajurilistaus, Composer-versio, cron-polku, palvelimen aikavyöhyke, SSH- ja SFTP-kirjautuminen, MariaDB-versiokysely, WordPress-juuren paikannus, `.htaccess`- ja LiteSpeed-tarkistus, aktiivisten lisäosien listaus sekä WordPressin HTTP-savukoe.
- **Tulokset ja todisteiden sijainti:** tämä REL-01-merkintä, päivitetty CloudCity-arkkitehtuurisuunnitelma sekä Fakiirimedian 17.8.2026 vastaanotettu vastaus. Kaikki REL-01:n tekniset tehtävät ja dokumentoitavat näytöt ovat valmiit.
- **WordPress-savukoe ennen/jälkeen:** tarkistukset 17.8.2026: `https://seniorsurf.fi/` palauttaa HTTP 200, lopullinen osoite säilyy HTTPS-etusivuna ja sivun otsikko on `SeniorSurf vahvistaa seniorien digiosallisuutta`. `https://seniorsurf.fi/wp-admin/` palauttaa kirjautumattomalle käyttäjälle HTTP 302 -ohjauksen polkuun `/wp-login.php`, ja kirjautumissivu palauttaa HTTP 200 otsikolla `Kirjaudu sisään ‹ SeniorSurf — WordPress`; kirjautumista ei tehty. Sovitut sisältösivut `https://seniorsurf.fi/seniorit/etaopastus/`, `https://seniorsurf.fi/ajankohtaista/` ja `https://seniorsurf.fi/seniorsurf/yhteystiedot/` palauttavat HTTP 200 ilman uudelleenohjausta ja otsikoilla `Etäopastus — SeniorSurf`, `Ajankohtaista — SeniorSurf` sekä `Yhteystiedot - SeniorSurf - työntekijät - etäopastuspalvelu`. Etusivun mediavertailu `https://seniorsurf.fi/wp-content/uploads/Artikkelikuva-etusivulle-1800x630-1.jpg` palauttaa HTTP 200 ilman uudelleenohjausta, sisältötyypin `image/jpeg` ja 152080 tavua. Julkinen WordPress-haku `https://seniorsurf.fi/?s=digiopastus` palauttaa HTTP 200, säilyttää hakukyselyn ja näyttää `Hakutulokset`-otsikon. PDO MySQL -moduulin aktivoinnin jälkeisessä savukokeessa ei havaittu näkyvää WordPress-regressiota. Näistä muodostetaan myöhempien ennen/jälkeen-savukokeiden vertailutaso.
- **Tietokanta- ja varmistusvaikutus:** stagingille ja tuotannolle tehdään omat loogiset tietokannat samalla MariaDB-palvelimella. Kummallekin tehdään oma sovelluskäyttäjä, jolla on vain oman tietokantansa normaalikäytön tarvitsemat `SELECT`-, `INSERT`-, `UPDATE`- ja `DELETE`-oikeudet. Skeemamigraatiot suoritetaan erillisillä ylläpito-oikeuksilla. Aloitussivun käyttäjillä ei ole oikeuksia WordPressin tietokantaan. Tietokannat ja käyttäjät luodaan vasta REL-02:ssa; REL-01:ssä hyväksytään suunnitelma. CloudCityn normaali varmistus kattaa kaiken hallintapaneelin `Website`-kansion alla olevan sisällön sekä tietokannat. Tiedostojen palautuspiste on 16.8.2026 klo 19.47 ja WordPress-tietokannan palautuspiste 17.8.2026; tietokannan kellonaika ei näy. Tietokannat voidaan palauttaa varmistuksesta itse käsin. Tuore palautuspiste tarkistetaan ennen REL-07:n staging-muutosta.
- **Avoimet P1/P2/P3-havainnot:** ei avoimia P1-havaintoja. Nina Ziessler hyväksyi väliaikaisen yhteisen SSH-tunnuksen P2-poikkeaman; henkilökohtaisiin tunnuksiin siirtyminen arvioidaan ennen tuotantojulkaisua.
- **Palautusohje:** ensisijainen palauttaja on Eero Tuomenoksa ja varahenkilö Nina Ziessler. WordPress-palautuksessa konsultoidaan Fakiirimediaa; CloudCityn varmistus- tai palvelinongelmassa käytetään CloudCityn tukea. Normaali varmistus sisältää `Website`-kansion ja tietokannat. Tietokannat voidaan palauttaa itse käsin, joten WordPressin ja Aloitussivun tietokantapalautukset voidaan tehdä erillisinä ja ne dokumentoidaan erikseen. Ennen tiedostojen palautusta tarkistetaan kaikki palautuksen korvaamat sivustohakemistot ja suojataan toisen sovelluksen mahdolliset uudemmat tiedostot. Tavoitteena on käynnistää palautus viipymättä ja saada palvelu palautettua enintään kahdessa tunnissa palautuspäätöksestä, kun hallintaportaalin varmistus on käytettävissä.
- **Seuraavalle paketille luovutettu:** REL-02:lle 17.8.2026 klo 14.45.
- **Tila:** valmis

## Paketti: REL-02

- **Päivämäärä:** 17.8.2026, valmistelu ennen työpaketin toteutuspäivää
- **Vastuuhenkilö:** Eero Tuomenoksa
- **Varahenkilö:** Nina Ziessler
- **Tarkistaja:** Nina Ziessler
- **Riippuvuudet tarkistettu:** kyllä; REL-01 on valmis ja staging- sekä tuotantotietokantojen eristysmalli on hyväksytty.
- **Muutokset ja päätökset:** versionoidut skeemamigraatiot sijoitetaan `database/migrations`-hakemistoon. Ensimmäinen migraatio on suunniteltu ajettavaksi erikseen staging- ja tuotantotietokantaan ylläpito-oikeuksilla; sovelluskäyttäjä ei saa DDL-oikeuksia.
- **Muokatut tiedostot tai palvelinasetukset:** lisätty `database/README.md` ja `database/migrations/001_initial_schema.sql`. CloudCityyn on luotu staging-tietokanta, jolle CloudCity loi automaattisesti tietokantakäyttäjän sekä tietokantakohtaiset oikeudet; tunnuksia tai salasanoja ei kirjata tähän tiedostoon. Migraatio on tuotu phpMyAdminilla ja kaikki 15 taulua näkyvät tietokannassa.
- **Suoritetut komennot ja testit:** migraation rakenne ja tiedostomuoto tarkistettu `git diff --check` -tarkistuksella. Työasemalla ei ole MariaDB- tai MySQL-asiakasta. Todellinen ajotesti suoritettiin 18.8.2026 phpMyAdminin tuonnilla tyhjään staging-tietokantaan onnistuneesti.
- **Tulokset ja todisteiden sijainti:** alkuskeema sisältää `schema_migrations`-, `admin_users`-, palaute-, testipalaute-, linkki-, huijausvaroitus-, NCSC-loki-, käyttötilasto-, rate-limit- ja auditointitaulut sekä niille määritellyt indeksit ja viiteavaimet. Versiomerkintä tarkistettiin phpMyAdminissa: `001_initial_schema`, `applied_at` `2026-08-18 05:26:35.677399` (UTC; EEST-ajassa 08:26:35).
- **Aikavyöhyketarkistus:** phpMyAdmin-istunnon `@@session.time_zone` on `SYSTEM`; `NOW(6)` oli kolme tuntia `UTC_TIMESTAMP(6)`-arvoa edellä. Tämä on odotettu CloudCityn EEST-palvelinaika. Sovelluksen jokainen PDO-yhteys asettaa ennen kyselyjä istunnon UTC:ksi komennolla `SET time_zone = '+00:00'`; vaatimus on lisätty migraatio-ohjeeseen.
- **UTC-istunnon varmennus:** `SET time_zone = '+00:00'` onnistui phpMyAdminissa 18.8.2026. Tämän jälkeen `@@session.time_zone` oli `+00:00` ja `NOW(6)` vastasi `UTC_TIMESTAMP(6)`-arvoa tarkalleen.
- **Tietokanta- ja varmistusvaikutus:** staging-tietokanta on perustettu ja alkuskeema on ajettu siihen. Palautustesti suoritettiin erilliseen CloudCity-tietokantaan `dbtmb_dbtma_aloitussivu_restore`: phpMyAdmin-tuonnin jälkeen `schema_migrations` sisälsi `001_initial_schema`-rivin alkuperäisellä aikaleimalla ja eksplisiittinen `information_schema`-kysely vahvisti 15 perustaulua. Versionoitu päivitystesti suoritettiin stagingissa migraatiolla `002_add_link_reports_triage_index.sql`. Nykyisen staging-tietokannan uusintatarkistuksessa 21.8.2026 indeksi ei aluksi löytynyt, joten migraatio 002 ajettiin siihen uudelleen. Lukutarkistus vahvisti tämän jälkeen yhden `002_add_link_reports_triage_index`-version ja indeksille kolme saraketta (`migration_002_count = 1`, `index_column_count = 3`). Tuotantotietokantaa ei ole vielä luotu tai muutettu.
- **Varmuuskopio:** phpMyAdmin-vienti luotiin 18.8.2026 tiedostona `dbtma_aloitussivu.sql` (14 430 tavua, paikallinen säilytys; ei Git-repositoriossa). Lukutarkistus vahvisti 15 `CREATE TABLE` -määritystä sekä `schema_migrations`-rivin `001_initial_schema` aikaleimalla `2026-08-18 05:26:35.677399`.
- **Avoimet P1/P2/P3-havainnot:** ei avoimia P1-havaintoja. P2: henkilökohtaiset SSH-tunnukset arvioidaan ennen tuotantojulkaisua REL-01:n päätöksen mukaisesti.
- **Seuraavalle paketille luovutettu:** REL-03:lle; staging-tietokanta, versionoidut migraatiot ja palautusmenettely ovat käytettävissä sovellusrajapinnan toteutukseen.
- **Tila:** valmis

## Paketti: REL-03

- **Päivämäärä:** 20.8.2026
- **Vastuuhenkilö:** Eero Tuomenoksa
- **Varahenkilö:** Nina Ziessler
- **Tarkistaja:** ei vielä nimetty
- **Riippuvuudet tarkistettu:** kyllä; REL-02 on valmis ja versionoitu MariaDB-skeema sekä staging-yhteysmalli ovat käytettävissä.
- **Muutokset ja päätökset:** toteutettu PHP 8.4 -bootstrap, PSR-4-autoload, tarkka reititin, ympäristökohtainen konfiguraation lataus, laiska PDO-yhteys, yhdenmukaiset JSON-virheet, request ID, tietosuojattu JSON-loki, pyyntökoon rajoitus, palvelinpuolen validointikehys, parametroidut kyselyt, tietokantapohjainen rate limit -perusta, HTTPS-vaatimus ja täsmällinen saman originin CORS. API ei lataa WordPressin tiedostoja, käytä WordPressin evästeitä tai muodosta yhteyttä WordPressin tietokantaan.
- **Muokatut tiedostot tai palvelinasetukset:** lisätty `api/bootstrap.php`, `api/src/`, `api/public/`, `api/config.example.php`, `api/tests/run.php` ja `api/README.md`; lisätty API:n salaisuus- ja lokihakemistot `.gitignore`-tiedostoon, PHP-tiedostot repositorion salaisuusskannaukseen sekä API-ohjeen linkki juuren `README.md`-tiedostoon. Cloudcityn, WordPressin tai tietokannan asetuksia ei muutettu.
- **Suoritetut komennot ja testit:** PHP 8.4.24 -lint kaikille API:n PHP-tiedostoille; `api/tests/run.php` SQLite PDO -ajurilla; paikallinen PHP HTTP -palvelin Gitistä ohitetulla SQLite-asetuksella; `npm run check:secrets`; `npm run build`; `git diff --check`.
- **Tulokset ja todisteiden sijainti:** kaikki PHP-tiedostot läpäisivät lintin ja 14/14 sopimustestiä läpäistiin. HTTP-kokeessa `GET /api/v1/health` palautti 200 ja tietokannan tilan `up`, puuttuva reitti 404/`not_found`, väärä metodi 405/`method_not_allowed` sekä `Allow: GET`, vieras origin 403/`origin_not_allowed`, sallittu preflight 204 ja kokorajan ylittävä pyyntö 413/`payload_too_large`. Kokonaan puuttuvalla asetustiedostolla entrypoint palautti 500/`configuration_error`, request ID:n eikä vuotanut tiedostopolkua. Asiakkaan kelvollinen request ID säilyi vastauksessa. SQL-injektiosyöte käsiteltiin parametrina eikä se muuttanut kyselyn rakennetta. Salaisuusskannaus ei löytänyt kovakoodattuja avaimia ja Vite-tuotantokäännös valmistui onnistuneesti. API-rakenne, ympäristösijoittelu, vastaussopimus, lokituksen tietosisältö ja testikomento on dokumentoitu tiedostossa `api/README.md`.
- **WordPress-savukoe ennen/jälkeen:** ei sovellu tähän paikalliseen koodipakettiin, koska WordPressin tiedostoja, palvelinasetuksia tai tuotantopalvelua ei muutettu. REL-07 tekee staging-asennuksen yhteydessä sovitun ennen/jälkeen-savukokeen.
- **Tietokanta- ja varmistusvaikutus:** Cloudcityn staging- tai tuotantotietokantaa ei muutettu eikä tuotantotunnuksia käytetty. Sopimustestit käyttivät muistissa olevaa SQLite-tietokantaa ja HTTP-koe Gitistä ohitettua paikallista SQLite-tiedostoa. Rate limit -komponentti käyttää REL-02:ssa luotua `rate_limit_buckets`-taulua vasta, kun julkiset reitit kytketään siihen.
- **Avoimet P1/P2/P3-havainnot:** ei avoimia REL-03:n P1-, P2- tai P3-havaintoja. Cloudcityn PHP 8.4.23 -staging-ajo, tiedosto-oikeudet, HTTPS ja lokin kierto varmennetaan REL-07:n eristetyssä staging-asennuksessa.
- **Palautusohje:** ennen palvelinasennusta muutos voidaan palauttaa poistamalla REL-03:ssa lisätty `api`-hakemisto ja palauttamalla `.gitignore`-, `README.md`- ja salaisuusskannausmuutokset. Tietokantapalautusta ei tarvita, koska tietokantoja ei muutettu.
- **Seuraavalle paketille luovutettu:** REL-04 voi lisätä hyväksyttyjen ja estettyjen linkkien, huijausvaroitusten sekä palautteiden reitit nykyisen reitittimen, validoinnin, PDO:n, virhemuodon, request ID:n, lokituksen ja rate limit -perustan päälle.
- **Tila:** valmis

## Paketti: REL-04

- **Päivämäärä:** 20.8.2026
- **Vastuuhenkilö:** Eero Tuomenoksa
- **Varahenkilö:** Nina Ziessler
- **Tarkistaja:** ei vielä nimetty
- **Riippuvuudet tarkistettu:** kyllä; REL-03 on valmis ja yhteinen reititys-, validointi-, PDO-, virhe-, request ID-, lokitus- ja rate limit -kerros on käytettävissä.
- **Muutokset ja päätökset:** toteutettu `GET /api/v1/approved-links`, `GET /api/v1/blocked-links`, `GET /api/v1/scam-alerts`, `POST /api/v1/link-reports`, `POST /api/v1/feedback`, `POST /api/v1/test-feedback` ja `POST /api/v1/usage-events`. Julkiset listat palauttavat vain dokumentoidut kentät, 60 sekunnin välimuistin ja ETagin. Kirjoitusreitit hyväksyvät vain nimetyt JSON-kentät, käyttävät honeypotia, reittikohtaista tietokantapohjaista pyyntörajoitusta ja UUID-pohjaista idempotenssia. Linkki-ilmoitus tunnistaa lisäksi saman tyypin ja normalisoidun URL:n avoimen duplikaatin.
- **Validointi ja liitteet:** Firestore-sääntöjen lomakeversiot, enumit sekä 40/80/120/140/160/180/255/500/900/1000/1200/1600 merkin rajat on siirretty palvelinvalidointiin käyttökohteen mukaan. Linkki-ilmoitus vaatii HTTPS-osoitteen. Palautteen valinnainen kuvakaappaus voi olla PNG, JPEG, WebP tai GIF ja enintään 450 KiB; palvelin tarkistaa base64-koon, kuvarakenteen ja MIME-tyypin. Kuva tallennetaan satunnaisella nimellä web-juuren ulkopuolelle, ja tietokantaan menevät vain suojattu tallennusavain sekä rajatut metatiedot ja SHA-256-tiiviste.
- **Tietosuoja:** käyttötilasto hyväksyy vain `pageview`- ja `linkClick`-kentät ja tallentaa Helsinki-päivän kokonais-, sivu- ja linkkilaskurit. Raakaa IP-osoitetta, käyttäjätunnistetta, user agentia tai selaimen sormenjälkeä ei hyväksytä eikä tallenneta käyttötauluihin. Rate limit käyttää asiakasosoitteesta palvelinsalaisuudella laskettua HMAC-SHA-256-tiivistettä. Palaute-, testipalaute-, linkki-ilmoitus- ja käyttöaineistoille ei ole julkisia lukureittejä.
- **Muokatut tiedostot tai palvelinasetukset:** laajennettu `api/src`-hakemiston tietokanta-, validointi-, rate limit- ja konfiguraatiokerroksia; lisätty `PublicApi`, suojattu liitetallennus, URL-, UUID-, kuvakaappaus- ja testipalautteen validaattorit; päivitetty `api/config.example.php`, `api/tests/run.php`, `api/README.md` ja `.gitignore`. Cloudcityn tai WordPressin tiedostoja, reititystä, välimuistia, palvelinasetuksia tai tietokantoja ei muutettu.
- **Suoritetut komennot ja testit:** PHP 8.4.24 -lint kaikille API:n PHP-tiedostoille; `api/tests/run.php` SQLite PDO -ajurilla; paikallinen PHP HTTP -palvelin Gitistä ohitetulla SQLite-fixturellä; `npm run check:secrets`; `npm run build`; `git diff --check`.
- **Tulokset ja todisteiden sijainti:** kaikki PHP-tiedostot läpäisivät lintin ja 23/23 sopimustestiä läpäistiin. Testit kattavat kolme julkista listaa, ETag/304:n, neljä kirjoitusreittiä, parametrisidonnan, linkkiduplikaatin, UUID-uusinnan, idempotenssiristiriidan, honeypotin ja 429-vastauksen, ylimittaiset sekä tuntemattomat kentät, naamioidun kuvatiedoston, liitteen tietokantarajauksen, käyttötilaston tietosuojan ja palauteaineistojen julkisen lukukiellon. HTTP-kokeessa hyväksyttyjen ja estettyjen linkkien listat palauttivat 200, hyväksyttyjen listan ETag-uusinta 304 ilman runkoa ja `GET /api/v1/feedback` palautti 405 sekä `Allow: POST`. Sopimukset ja anonyymit esimerkkipyynnöt on dokumentoitu tiedostossa `api/README.md`.
- **WordPress-savukoe ennen/jälkeen:** ei sovellu tähän paikalliseen koodipakettiin, koska staging- tai tuotantopalvelimelle, WordPressiin tai LiteSpeed Cacheen ei tehty muutoksia. LiteSpeed vaikuttaa vain WordPress-asennukseen. REL-07 tekee staging-asennuksen yhteydessä sovitun ennen/jälkeen-savukokeen.
- **Tietokanta- ja varmistusvaikutus:** Cloudcityn tietokantoja tai skeemaa ei muutettu. Reitit käyttävät REL-02:n olemassa olevia tauluja ja parametroitua SQL:ää. Sopimustestit käyttivät muistirajapintoja ja PDO-parametrisidonnan SQLite-testiä; HTTP-koe käytti Gitistä ohitettua paikallista SQLite-fixtureä. Cloudcityn normaali varmistus kattaa `Website`-kansion ja tietokannat, ja tietokannat voidaan palauttaa myös itse käsin.
- **Avoimet P1/P2/P3-havainnot:** ei avoimia REL-04:n P1-, P2- tai P3-havaintoja. MariaDB 12 -kyselyt, tiedosto-oikeudet ja suojatun liitehakemiston todellinen palvelinsijoittelu varmennetaan REL-07:n staging-asennuksessa.
- **Palautusohje:** ennen palvelinasennusta REL-04 voidaan palauttaa poistamalla julkisten reittien ja validaattorien lisäykset sekä palauttamalla REL-03:n `App`, `Config`, tietokanta- ja rate limit -rajapinnat. Tietokantapalautusta ei tarvita, koska skeemaa tai Cloudcity-dataa ei muutettu.
- **Seuraavalle paketille luovutettu:** REL-05 saa vakaat julkiset vastaussopimukset, yhteisen reitittimen ja virhemuodon, PDO-parametrisidonnan, palvelinvalidoinnin, suojatun liitetallennuksen, request ID:n, tietosuojatun lokituksen, idempotenssin ja pyyntörajoituksen. Ylläpidon lukureitit voivat käyttää samoja tauluja paljastamatta niitä julkisessa reitittimessä.
- **Tila:** valmis

## Paketti: REL-05

- **Päivämäärä:** 21.8.2026
- **Vastuuhenkilö:** Eero Tuomenoksa
- **Varahenkilö:** Nina Ziessler
- **Tarkistaja:** ei vielä nimetty
- **Riippuvuudet tarkistettu:** kyllä; REL-04 on valmis ja julkisten reittien sopimukset sekä MariaDB-skeeman `admin_users`- ja `audit_log`-taulut ovat käytettävissä.
- **Muutokset ja päätökset:** toteutettu Firebase Client SDK:n ID-tokenin palvelinpuolinen RS256-varmennus, Googlen `securetoken`-varmenteiden `Cache-Control`-ohjattu välimuisti sekä `aud`-, `iss`-, `exp`-, `iat`-, `auth_time`- ja `sub`-väitteiden tarkistus. Oikeus sidotaan varmennettuun `sub`-UID:hen sekä saman UID:n aktiiviseen `viewer`-, `editor`- tai `admin`-rooliin `admin_users`-taulussa. Valinnaiset sähköposti- ja provider-väitteet eivät estä provisioitua UID:tä, eikä selainpuolen sähköpostilla, localStorage-arvolla, WordPress-evästeellä tai muulla asiakasväitteellä saa oikeuksia. `viewer` saa lukea, `editor` ja `admin` saavat tehdä sisältömuutoksia.
- **Ylläpitoreitit:** lisätty `/api/v1/admin/me`, linkki-ilmoitusten, avoimen palautteen, suojattujen palauteliitteiden, testipalautteen, hyväksyttyjen ja estettyjen linkkien, kaikkien huijausvaroitusten, NCSC-ajolokien, käyttötilastoaggregaattien ja auditointilokin lukureitit. Lisätty linkki-ilmoitusten ja palautteiden rajatut `PATCH`-reitit sekä hyväksyttyjen linkkien, estettyjen linkkien ja huijausvaroitusten `POST`-/`PATCH`-/`DELETE`-reitit suunnitellun sopimuksen mukaisesti. Reititin tukee nyt täsmällisiä `{id}`-polkuparametreja ja oikeita `Allow`- sekä preflight-vastauksia. `POST /api/v1/admin/ncsc-run` toteutetaan vasta REL-09:ssä yhdessä turvallisen ajon, lukituksen ja idempotenssin kanssa; REL-05 ei lisää näennäistä käsiajoa.
- **Auditointi ja tietosuoja:** jokainen toteutettu ylläpidon muutos tekee varsinaisen muutoksen ja `audit_log`-rivin samassa tietokantatransaktiossa. Auditointiin tallennetaan tekijän UID, toiminto, kohdetyyppi, kohdetunniste ja muuttuneiden kenttien nimet, mutta ei palautetekstiä, käsittelyperustetta, tokenia tai muuta yksityistä kenttäarvoa. Ylläpitovastaukset käyttävät `Cache-Control: private, no-store`. Liitteen sisäistä tallennusavainta ei palauteta asiakkaalle, ja tiedosto luetaan web-juuren ulkopuolelta vasta uuden oikeustarkistuksen jälkeen.
- **Roolilinjaus:** ympäristöihin ei lisätä uusia ylläpitäjiä REL-05:n yhteydessä. Nykyiset hyväksytyt, kaksivaiheisesti suojatut Firebase-ylläpitotilit provisioidaan stagingin ja tuotannon `admin_users`-tauluihin UID:n ja vahvistetun sähköpostin perusteella repositorion ulkopuolisessa ylläpitoistunnossa. Stagingiin provisioitiin 21.8.2026 kaksi hyväksyttyä ylläpitäjää roolilla `admin` ja aktiivisuudella `1`; koontitarkistus palautti `total_rows = 2`, `admin_rows = 2` ja `active_rows = 2`. Ympäristökohtaisia UID:itä tai täytettyjä provisiointikomentoja ei tallenneta Gitiin. Provisiointimalli ja oikeuden välitön poistaminen käytöstä on dokumentoitu `database/README.md`-tiedostossa.
- **Muokatut tiedostot tai palvelinasetukset:** lisätty ylläpidon API-, tunnistus-, Firebase-token-, julkisten avainten välimuisti- ja käyttäjäoliot `api/src`-hakemistoon; laajennettu `App`, `Config`, `Router`, `Request` ja liitetallennus; päivitetty `api/config.example.php`, `api/tests/run.php`, `api/README.md`, `database/README.md` ja `.gitignore`. Eero vahvisti 21.8.2026 lisänneensä Firebase Authenticationiin staging- ja tuotantodomainit sekä Google Cloudin Firebase-selainavaimelle vastaavat HTTP-referrer-rajaukset nykyiset testausoriginit säilyttäen. Samana päivänä staging-tietokantaan ajettiin ja varmennettiin puuttunut migraatio 002. Cloudcityn tiedosto- tai WordPress-asetuksia ei muutettu.
- **Suoritetut komennot ja testit:** PHP 8.4.24 -lint kaikille API:n 37 PHP-tiedostolle; 33 sopimus- ja oikeustestiä SQLite PDO- ja OpenSSL-laajennuksilla; paikallinen PHP HTTP -savukoe; `npm run check:secrets`; `npm run build`; `git diff --check`.
- **Tulokset ja todisteiden sijainti:** kaikki 37 PHP-tiedostoa läpäisivät lintin ja 33/33 testiä läpäistiin. Testit allekirjoittavat paikallisen RSA-avaimen RS256-tokenin ja kattavat oikean allekirjoituksen, väärennetyn tokenin, väärän projektin ja issuerin, vanhentuneen tokenin, puuttuvan tunnistuksen, nimeämättömän käyttäjän, passiivisen roolin, hyväksytyn UID:n myös valinnaisten identiteettiväitteiden puuttuessa, hyväksytyn ylläpitäjän, vain luku -roolin, parametroidun muutoksen, auditoinnin, suojatun liiteluvun, stagingin rinnakkaiset Basic Auth- ja Firebase-token-otsakkeet sekä dynaamisen reitin metodi- ja CORS-sopimukset. Paikallisessa HTTP-kokeessa health palautti 200, kirjautumaton `/api/v1/admin/me` palautti 401/`authentication_required`, dynaamisen linkki-ilmoitusreitin väärä metodi 405 ja `Allow: PATCH`, ja Authorization-otsakkeen salliva preflight 204. Salaisuusskannaus ja Vite-tuotantokäännös valmistuivat onnistuneesti. Sopimukset, roolit, virheet ja provisiointiperiaate on dokumentoitu `api/README.md`- ja `database/README.md`-tiedostoissa.
- **WordPress-savukoe ennen/jälkeen:** ei sovellu tähän paikalliseen koodiosaan, koska staging- tai tuotantopalvelimelle, WordPressiin tai LiteSpeed Cacheen ei tehty muutoksia. REL-07 tekee staging-asennuksen yhteydessä sovitun ennen/jälkeen-savukokeen.
- **Tietokanta- ja varmistusvaikutus:** staging-tietokantaan ajettiin 21.8.2026 migraatio 002 ja sen versiorivi sekä kolmisarakkeinen indeksi varmennettiin lukukyselyllä. Samana päivänä `admin_users`-tauluun lisättiin kaksi aktiivista, hyväksyttyä Firebase-ylläpitäjää. Tuotantotietokantaa ei muutettu. Toteutus käyttää REL-02:n nykyisiä `admin_users`- ja `audit_log`-tauluja. Paikalliset testit käyttivät muistirajapintoja ja SQLite-parametrisidontaa; MariaDB 12 -kyselyt ja transaktioauditointi varmennetaan stagingissä ennen hyväksyntää.
- **Avoimet P1/P2/P3-havainnot:** ei avointa ylläpitotunnistuksen teknistä estettä; kaksi hyväksyttyä Google-ylläpitäjää, passiivisen roolin esto ja oikeuden palautus sekä 33/33 PHP-sopimustestiä on varmennettu 24.8.2026. Uusia TEST-P2-seniorituloksia ei ollut saapunut, joten tuotevastuu siirsi niiden koonnin ja P1/P2-listan lukituksen REL-10:een; siirto ei estä REL-05:n teknistä hyväksyntää.
- **Palautusohje:** ennen ympäristökytkentää REL-05 voidaan palauttaa poistamalla uudet ylläpito- ja Firebase-tunnistusluokat sekä palauttamalla `App`, `Config`, `Router`, `Request`, liitetallennus, testi- ja dokumentaatiomuutokset REL-04:n tilaan. Tietokantapalautusta tai Firebase-palautusta ei tarvita, koska ulkoista ympäristöä ei muutettu.
- **Seuraavalle paketille luovutettu:** REL-06:n provider-rajapinnan julkiset ja ylläpidon API-sopimukset, hyväksytyn ja passiivisen UID:n staging-näyttö sekä 33/33 PHP-testit ovat valmiit. Tuotevastuu hyväksyi TEST-P2-koonnin siirron REL-10:een 24.8.2026.
- **Tila:** valmis

## Paketti: REL-06

- **Päivämäärä:** 21.8.2026 (ennakkototeutus; suunniteltu pakettipäivä 24.8.2026)
- **Vastuuhenkilö:** Eero Tuomenoksa
- **Varahenkilö:** Nina Ziessler
- **Tarkistaja:** ei vielä nimetty
- **Riippuvuudet tarkistettu:** kyllä; REL-05:n API-sopimukset, ympäristökohtainen tunnistuskoe ja PHP-testit ovat valmiit. Tuotevastuu siirsi puuttuvien TEST-P2-tulosten koonnin REL-10:een.
- **Muutokset ja päätökset:** kaikki julkaisuun kuuluvat julkiset ja ylläpidon tietovirrat siirrettiin yhden `DataProvider`-rajapinnan taakse. Cloudcity on ensisijainen julkaisu- ja staging-provider saman originin `/api/v1`-rajapintaan. Firebase/Firestore säilyy erikseen käännettävänä määräaikaisena palautusvaihtoehtona ja local-provider kehitys- sekä verkkovirhejonona. Cloudcity-build käyttää käännösaikaista provider-aliasointia, joten Firestore-datamoduuleja ei sisälly julkaistavaan tietokerrokseen. Firebase Authentication säilyy ylläpitäjän kirjautumisessa; käyttöoikeus syntyy vasta palvelimen `/api/v1/admin/me`-vastauksesta. Reaaliaikaiset Firestore-kuuntelut korvattiin alkulatauksella, muutoksen jälkeisellä päivityksellä ja ylläpitonäkymien hallitulla 30 sekunnin kyselyvälillä.
- **Käyttäjälle näkyvä toiminta:** palaute, testipalaute ja linkki-ilmoitus lähetetään Cloudcity-API:in. Verkkovirheessä lähetys säilyy selaimen näkyvässä paikallisessa jonossa samalla UUID-tunnisteella myöhempää idempotenttia uudelleenlähetystä varten. Palautelomakkeen tietosuojateksti kertoo, että sisältö näkyy vain hyväksytylle ylläpidolle; kirjautumaton käyttäjä ei näe ylläpidon palautelistaa.
- **Muokatut tiedostot tai palvelinasetukset:** lisätty `services/data`-hakemiston provider-rajapinta, asetukset sekä Cloudcity-, Firebase-palautus- ja local-adapterit; lisätty `.env.cloudcity`, `.env.staging`, `.env.firebase-rollback` ja `.env.local-provider`; laajennettu Vite-konfiguraatio ja build-komennot. Providerin kautta kulkeviksi muutettiin hyväksytyt ja estetyt linkit, linkki-ilmoitukset, palaute, testipalaute, huijausvaroitukset, käyttötilastot ja ylläpidon tilastonäkymät. Cloudcityn palvelintiedostoja, tietokantaa, WordPressiä tai Firebase-asetuksia ei muutettu tässä paketissa.
- **Suoritetut komennot ja testit:** `npx tsc --noEmit`; `npm run build:cloudcity`; `npm run build:staging`; `npm run build:firebase-rollback`; `npm run build:local-provider`; `npm run check:secrets`; `git diff --check`; PHP API:n 33 sopimus- ja oikeustestiä. Lisäksi local- ja Cloudcity-providerit testattiin oikeassa Chromium-selaimessa paikallisilla Vite-palvelimilla ja Cloudcity-API:n sopimusvastauksilla.
- **Tulokset ja todisteiden sijainti:** kaikki neljä frontend-buildia valmistuivat. Cloudcity-bundlesta ei löytynyt Firestore-endpointtia, Firestore RPC -tunnistetta, `firebaseDataProvider`-viitettä tai `onSnapshot`-kuuntelua. Selainkoe vahvisti `POST /api/v1/feedback` -pyynnön oikeaan saman originin osoitteeseen ilman palvelimen hallitsemia `status`-, `createdAt`- tai `updatedAt`-kenttiä. Paikallinen varajono sai saman `createdAt`- ja `updatedAt`-ajan. Kirjautumaton kehitysjono näytti kirjautumisvaatimuksen eikä hakenut yksityistä palautelistaa. PHP API:n testitulos oli 33/33, ja oikean stagingin positiivinen sekä passiivisen roolin negatiivinen ylläpitokoe läpäisivät.
- **WordPress-savukoe ennen/jälkeen:** ei sovellu tähän paikalliseen koodipakettiin, koska Cloudcityn tai WordPressin tiedostoja ja asetuksia ei muutettu. REL-07 tekee sovitun ennen/jälkeen-savukokeen staging-asennuksessa.
- **Tietokanta- ja varmistusvaikutus:** ei uusia skeema- tai datamuutoksia. Stagingin migraatiot 001 ja 002 sekä kaksi aktiivista admin-roolia pysyvät ennallaan. Migraatioiden ja admin-roolien varmistamisen jälkeinen uusi phpMyAdmin-SQL-vienti otettiin 21.8.2026 ja säilytetään paikallisesti Git-repositorion ulkopuolella REL-07:n palautuspisteenä.
- **Avoimet P1/P2/P3-havainnot:** ei avointa provider-siirtymän teknistä estettä; Cloudcity-providerin MariaDB-luku, julkiset sivut, kahden ylläpitäjän kirjautuminen ja passiivisen roolin esto on varmennettu stagingissä. TEST-P2 siirrettiin tuotevastuupäätöksellä REL-10:een. NCSC-käsiajo toteutetaan suunnitelman mukaisesti vasta REL-09:ssä.
- **Palautusohje:** frontend voidaan kääntää määräaikaiseen Firestore-palautustilaan komennolla `npm run build:firebase-rollback`. Cloudcityn kirjoitukset on pysäytettävä hallitusti ja datan täsmäytys tehtävä ennen ympäristövaihtoa; pelkkä buildin vaihtaminen ei siirrä uusia Cloudcity-tietueita Firestoreen.
- **Seuraavalle paketille luovutettu:** REL-07 saa staging-buildin komennolla `npm run build:staging`, saman originin `/api/v1`-kytkennän ja dokumentoidun Firebase-palautusbuildin. Ennen asennusta tarkistetaan tuore Cloudcity-palautuspiste, ja asennuksen jälkeen suoritetaan oikeat julkiset sekä ylläpidon integraatiotestit ja WordPress-savukoe.
- **Tila:** valmis

## Paketti: REL-07

- **Päivämäärä:** 21.8.2026 (paikallinen ennakkovalmistelu; suunniteltu ympäristöpäivä 25.8.2026)
- **Vastuuhenkilö:** Eero Tuomenoksa
- **Varahenkilö:** Nina Ziessler
- **Tarkistaja:** nimetään ennen staging-ympäristön muutosta
- **Riippuvuudet tarkistettu:** kyllä; REL-01, REL-05 ja REL-06 ovat valmiit, staging-muutosikkuna on hyväksytty päivälle 25.8. ja TEST-P2-koonti on siirretty tuotevastuupäätöksellä REL-10:een.
- **Varmistus ennen muutosta:** migraatiot 001 ja 002 sekä kaksi aktiivista admin-roolia sisältävä staging-tietokannan phpMyAdmin-vienti otettiin 21.8.2026 ja säilytetään paikallisesti Git-repositorion ulkopuolella. WordPressin tiedostoja tai tietokantaa ei muutettu.
- **WordPress-savukoe ennen muutosta:** julkinen tarkistus 21.8.2026 vahvisti etusivun HTTP 200 -vastauksen ja aiemman SeniorSurf-sisältömerkin, `/wp-admin/`-osoitteen HTTP 302 -ohjauksen kirjautumissivulle sekä kirjautumissivun HTTP 200 -vastauksen. Etäopastus-, Ajankohtaista- ja Yhteystiedot-sivut, julkinen haku ja sovittu JPEG-mediatiedosto palauttivat HTTP 200; haku sisälsi Hakutulokset-merkin ja media oli JPEG. Näkyvää eroa REL-01:n lähtötasoon ei havaittu.
- **Paikallinen toteutus:** lisätty saavutettava `404.html`, stagingin `noindex`-metatiedot, erillinen Cloudcityn pääjuuren `.htaccess`-malli, HTTPS-ohjaus, HSTS, CSP, `X-Content-Type-Options`, `Referrer-Policy`, kehystysrajaus, Permissions Policy, gzip, HTML:n uudelleenvalidointi ja hashattujen resurssien vuoden immutable-välimuisti. API:n `.htaccess` pakottaa `/api/`-vastaukset `no-store`-tilaan. Lisätty salaisuuksia sisältämätön staging-konfiguraatiomalli ja PowerShell-paketointikomento.
- **Siirtopaketti:** `C:\dev\Aloitussivu\.tmp\aloitussivu-rel07-staging.zip`; versio `0.73.1`, Git HEAD `2c0c9e717256`, työpuu tarkoituksellisesti keskeneräinen ja yksilöivä ZIP SHA-256 `3120f3e51cbf1f348c6a252ce145f87ac68d93fa9b5492095c9b541a91f4fab1`. Pakettia käytetään vain staging-integraation harjoitukseen; lopullinen julkaisukandidaatti rakennetaan myöhemmin hyväksytystä ja yksilöidystä lähdetilasta.
- **Paketin sisältötarkistus:** ZIP käyttää Linux-yhteensopivia kauttaviivapolkuja ja sisältää 109 tiedostomerkintää. Mukana ovat pääjuuren ja API:n `.htaccess`, `404.html`, frontend, API-entrypoint, bootstrap ja `src` sekä vain `config.staging.example.php`. Mukana ei ole oikeaa `secrets/config.php`-tiedostoa, `.env`-tiedostoja tai API-testejä. Kaikki 11 HTML-tiedostoa sisältävät `noindex`-rajauksen. Cloudcity-staging-build ei sisällä Firestore-dataprovideria tai reaaliaikakuuntelua.
- **Suoritetut komennot ja testit:** WordPressin julkinen HTTP-savukoe; `npx tsc --noEmit`; `npm run build:staging`; `powershell -ExecutionPolicy Bypass -File scripts/build-rel07-staging-package.ps1`; kaikkien pakettiin sisältyvien PHP-tiedostojen PHP 8.4.24 -lint; ZIP-rakenne-, noindex-, CSP/auth-domain- ja sisältötarkistus; `npm run check:secrets`; `git diff --check`.
- **Cloudcity-asennus 21.8.2026:** `/website.aloitussivu-staging/` luotiin, paketti purettiin sinne ja `staging.aloitussivu.seniorsurf.fi` ohjattiin hakemiston `public_html`-alikansioon. Myös Cloudcityn testiosoite osoittaa samaan kohteeseen. Staging suojattiin yhdellä HTTP Basic Auth -käyttäjällä; kirjautumaton HTTPS-pyyntö palauttaa 401-vastauksen ja TLS toimii. Sovellus, yksityiset `secrets`, `src`, `logs`, `cache` ja `protected_uploads` sekä julkinen `public_html` ovat erillään suunnitellulla tavalla.
- **Konfiguraatio ja tietokantayhteys:** palvelimelle luotiin oikea `secrets/config.php` esimerkkitiedoston kopiosta ja sen oikeudeksi asetettiin `640`. Ensimmäinen health-koe palautti `service_unavailable`/`database: down`, koska tietokantaisäntänä kokeiltiin virheellisesti `127.0.0.1`. Cloudcityn oikealla tietokantaisännällä `dbtma.db.cchosting.fi` `/api/v1/health` palautti `status: ok`, `database: up` ja version `v1`. Isäntä korjattiin myös paikalliseen konfiguraatiomalliin ja asennusohjeeseen; tunnuksia tai salaisuuksia ei kirjata Gitiin tai tähän päiväkirjaan.
- **Julkiset staging-kokeet:** frontend latautui osoitteessa `https://staging.aloitussivu.seniorsurf.fi/`. `GET /api/v1/approved-links` palautti onnistuneen tyhjän tietojoukon, mikä vahvisti oikean MariaDB-lukuyhteyden. Linkkisivu toimi ja tarkoituksella puuttuva URL palautti oman 404-sivun.
- **Firebase- ja CSP-kokeet:** Google-kirjautuminen epäonnistui aluksi Firebase-virheeseen. Selainkonsoli osoitti, että CSP esti `https://apis.google.com/js/api.js`-skriptin. Stagingin julkiseen `.htaccess`-tiedostoon ja paikalliseen malliin lisättiin `https://apis.google.com` skriptilähteeksi ja `https://www.googleapis.com` yhteyskohteeksi. Tämän jälkeen Google-popup-kirjautuminen onnistui. Erillinen Firebase REST -koe staging-referrerillä vahvisti, että selainavain hyväksytään, Google-provider vastaa ja staging-domain on sallittu.
- **Ylläpitotunnistuksen nykytila:** stagingin MariaDB-tarkistus palautti nykyiselle Google-provider-käyttäjälle UID-, sähköposti- ja aktiivisuus/rooliosumaksi `1 / 1 / 1`. Firebase `accounts:lookup` palautti sähköpostin mutta ei `emailVerified`-kenttää, joten palvelin hylkää ID-tokenin suunnitellusti ylläpito-oikeudettomana. Samalle sähköpostille löytyi erillinen password-provider-käyttäjä. Identity Platformin virallisella `projects.accounts.update` -kokeella vahvistetuksi merkittiin erehdyksessä tämä password-provider-käyttäjä, ei MariaDB:ssä sallittu Google-provider-käyttäjä. Password-käyttäjä ei saanut stagingin ylläpito-oikeutta, koska sen UID ei vastaa sallittua riviä. Käyttäjien UID:itä tai sähköpostia ei tallenneta tähän päiväkirjaan.
- **Paikalliset korjaukset asennuksen aikana:** Cloudcityn konfiguraatiomallin tietokantaisäntä, asennusohjeen todellinen hakemistopolku ja CSP-malli korjattiin. Ylläpitonäkymän vanhentuneet GitHub Pages -kohtaiset Firebase-virheohjeet korvattiin nykyisen domainin ja CSP:n huomioivilla ohjeilla. Koska paketin lähdemallit muuttuivat palvelinasennuksen aikana, aikaisempaa ZIP-tiivistettä ei käsitellä lopullisen luovutuspaketin tiivisteenä; staging-paketti rakennetaan ja tarkistetaan uudelleen ennen luovutusta.
- **Tietokanta- ja palvelinvaikutus:** staging-alidomainin kohde, Basic Auth, Aloitussivun staging-hakemisto, yksityinen konfiguraatio ja julkisen juuren CSP muuttuivat. Staging-tietokannan skeemaa tai dataa ei muutettu tässä asennusvaiheessa, eikä WordPressin tiedostoja, asetuksia tai tietokantaa muutettu. Ennen asennusta otettu stagingin SQL-vienti säilyy Git-repositorion ulkopuolella.
- **Avoimet P1/P2/P3-havainnot:** ei avoimia REL-07:n P1-havaintoja. Nykykoodin julkiset tiedostot ja hashatut resurssit on synkronoitu, molemmat ylläpitäjät ja käytöstä poistetun roolin esto on testattu sekä ympäristön uusintakokeet on läpäisty. TEST-P2:n uusien senioritulosten koonti ja P1/P2-listan lukitus on tuotevastuupäätöksellä siirretty REL-10:een eikä estä REL-07:n hyväksyntää.
- **Palautusohje:** stagingin Aloitussivu voidaan palauttaa poistamalla alidomainin ohjaus tai vaihtamalla se takaisin tyhjään staging-kohteeseen ja palauttamalla vain `/website.aloitussivu-staging/` sitä edeltävään tilaan. Tietokanta palautetaan tarvittaessa 21.8. otetusta viennistä. WordPressin hakemistoa tai tietokantaa ei palauteta, koska niitä ei muutettu. Firebase-käyttäjien muutokset käsitellään käyttäjäkohtaisesti vasta providerin ja UID:n varmistamisen jälkeen.
- **Jatkopiste maanantaille 24.8.2026:** aloita tarkistamalla Identity Platform API -pyynnön kohteeksi nimenomaan staging-virheessä näkyvä Google-provider-käyttäjä, aseta sen `emailVerified: true`, kirjaudu stagingistä kokonaan ulos ja takaisin sisään uuden ID-tokenin saamiseksi ja varmista `/api/v1/admin/me`-vastauksesta rooli `admin`. Selvitä sen jälkeen erillisen password-provider-käyttäjän turvallinen käsittely tekemättä uusia käyttäjämuutoksia ennen providerin ja UID:n varmistusta. Jatka hyväksytyn/poistetun roolin kokeeseen, runtime-oikeuksiin, otsake-/välimuisti-/pakkaustarkistuksiin, WordPressin jälkeen-savukokeeseen sekä paketin uudelleenrakennukseen ja palvelin-ZIPin poistoon.
- **Aikatauluarvio 24.8.2026:** keltainen mutta vielä aikataulussa. REL-07:n suunniteltu ympäristöpäivä on 25.8., joten ylläpitotunnistus ja jäljellä olevat ympäristötarkistukset on saatava valmiiksi 24.8. P1-tunnistusvirhe tai TEST-P2-lukituksen viivästyminen siirtää muodollista hyväksyntää, vaikka julkinen staging-palvelu jo toimii.
- **Jatkotesti 24.8.2026:** Google-popup-kirjautuminen onnistuu nyt kummallakin tarkoitetulla ylläpitotunnuksella, mutta staging palauttaa kummallekin edelleen `admin_forbidden`-tilan ja ilmoituksen, ettei ylläpito-oikeutta ole. Tulos rajaa CSP-, provider- ja popup-ongelman pois, mutta ei vielä erota puuttuvaa `email_verified`-väitettä UID-, sähköposti-, aktiivisuus- tai rooliosumasta. Seuraavaksi tarkistetaan yhden tunnuksen tuoreesta `accounts:lookup`-vastauksesta provider ja `emailVerified`, minkä jälkeen sama tunnus verrataan stagingin `admin_users`-riviin.
- **Ylläpitonäkymien virhe-esitys 24.8.2026:** testipalautteen koonti näyttää kirjautumisen jälkeen lyhyen ei käyttöoikeutta -tilan, kun taas linkki-, tilasto- ja muu ylläpitonäkymä näyttää saman palvelinvirheen lisäksi Firebase-käyttäjän diagnostiikan. Kooditarkistus vahvisti, että näkymät kutsuvat samaa Cloudcityn `/api/v1/admin/me`-oikeustarkistusta; ero on vain käyttöliittymän virhe-esityksessä. Diagnostiikassa Google-provider tunnistuu, mutta sähköpostin lähteistä puuttuu `user.email` ja näkyvissä ovat vain `providerData.email` sekä popup-profiilin sähköposti. Palvelin ei luota näihin selainlähteisiin, vaan vaatii ID-tokenin varsinaisen `email`- ja `email_verified`-väitteen. Tämä sopii yhteen erillisen password-provider-kaksoiskäyttäjän kanssa ja tarkistetaan ennen käyttäjätilien yhdistämistä tai poistamista.
- **Google-käyttäjän tietuetarkistus 24.8.2026:** yhden Google-provider-käyttäjän `accounts:lookup`-vastauksessa sähköposti näkyi käyttäjäobjektin ylätasolla, mutta `emailVerified` puuttui. Käyttäjän aikaisempi staging-tietokannan koontitulos oli kuitenkin `0 / 0 / 0`, joten tällä UID:llä ei ollut MariaDB:n `admin_users`-osumaa. Firebase-vahvistus ja tietokantaprovisiointi käsitellään erillisinä ehtoina; käyttäjää ei nimetä hyväksytyksi ylläpitäjäksi pelkän sähköpostin tai providerin perusteella.
- **Google-käyttäjän vahvistus ja uusintakoe 24.8.2026:** kyseinen `google.com`-provider-käyttäjä päivitettiin virallisella Identity Platform `projects.accounts.update` -pyynnöllä. Vastauksesta tarkistettiin sama `localId`, provider `google.com` ja `emailVerified: true`. Kokonaan uuden ulos- ja sisäänkirjautumisen jälkeen staging palautti edelleen `admin_forbidden`, mikä on odotettu tulos ilman tämän UID:n `admin_users`-riviä. Ennen tietokantaoikeuden lisäämistä on vahvistettava erikseen, että juuri tämä Google-käyttäjä kuuluu kahteen ennalta hyväksyttyyn ylläpitäjään.
- **MariaDB-osuman uusintatarkistus 24.8.2026:** käyttäjä vahvistettiin yhdeksi kahdesta ennalta hyväksytystä ylläpitäjästä. Sähköpostilla rajattu koontikysely palautti `email_rows = 1`, `matching_uid_rows = 1`, `admin_rows = 1` ja `active_rows = 1`. Tietokantariviä ei siksi muutettu. Koska kaikki tietokantaehdot täsmäävät mutta palvelin palauttaa edelleen 403:n, Cloudcity-providerin ensimmäinen `/api/v1/admin/me`-pyyntö muutettiin pakottamaan tuore Firebase ID-token; myöhemmät pyynnöt voivat käyttää tätä uutta tokenia normaalisti.
- **Token-korjauksen tarkistuspaketti 24.8.2026:** `npx tsc --noEmit`, staging-build ja `git diff --check` läpäistiin. PHP-sopimustestejä ei voitu ajaa tässä työasemaistunnossa, koska PHP-komentorivityökalu ei ollut PATH:ssa; PHP-lähdekoodia ei muutettu. Uusi staging-ZIP rakennettiin polkuun `C:\dev\Aloitussivu\.tmp\aloitussivu-rel07-staging.zip`, SHA-256 `9497b798b000231a585c825e44f1e4a369063e952c777170f59c5c8fc797137d`. ZIP sisältää vain `secrets/config.staging.example.php`-mallin, ei oikeaa `secrets/config.php`-tiedostoa. Bundle-tarkistus vahvisti, että `/admin/me` käyttää pakotettua token-päivitystä.
- **Cloudcity-siirtotavan tarkennus 24.8.2026:** uuden ZIPin lataaminen onnistui, mutta Cloudcityn selainpohjaisessa tiedostonhallinnassa ei ollut käytettävissä toimivaa ZIP-purkua. Pakettia ei yritetä purkaa siellä uudelleen. Ensisijainen jatkotapa on purkaa jo ladattu ZIP SSH-istunnossa vasta sen jälkeen, kun `pwd` vahvistaa kohteeksi stagingin `/website.aloitussivu-staging`-hakemiston. Varavaihtoehto on siirtää SFTP:llä paikallisen `C:\dev\Aloitussivu\.tmp\rel07-staging-package\public_html`-hakemiston sisältö suoraan palvelimen `public_html`-hakemistoon. Kumpikaan tapa ei korvaa palvelimen yksityistä `secrets/config.php`-tiedostoa.
- **SSH-yhteyskokeilu 24.8.2026:** ensimmäinen Windows PowerShellin `ssh`-komento annettiin ilman etäkäyttäjää, jolloin OpenSSH käytti automaattisesti paikallista Windows-domainkäyttäjää; palvelin hylkäsi kolme salasanayritystä. Tämän jälkeen käyttäjän sisältävä yhteysmuoto ei enää saanut TCP-yhteyttä porttiin 22, ja erillinen `Test-NetConnection` vahvisti portin aikakatkaisun osoitteeseen `185.220.77.13`. Uusia kirjautumisyrityksiä ei tehdä ennen kuin Cloudcityn SSH ja SFTP -välilehdeltä on kopioitu täsmällinen käyttäjätunnus ja portti sekä erillinen SSH/SFTP-salasana on vahvistettu aktiiviseksi. Virheen muuttuminen hylätystä salasanasta aikakatkaisuksi voi viitata tilapäiseen IP-estoon, mutta tätä ei ole vahvistettu Cloudcityltä.
- **Frontend-päivitys ilman ZIP-purkua 24.8.2026:** Cloudcityn selainpohjaisella tiedostonhallinnalla ladattiin paikallisen staging-buildin kaikki 50 tiedostoa palvelimen `/website.aloitussivu-staging/public_html/assets`-hakemistoon sekä uudet `ehdotukset.html`, `kehitysjono.html` ja `testipalaute-yllapito.html` palvelimen `public_html`-hakemistoon. Samannimiset julkiset tiedostot korvattiin. Palvelimen `secrets/config.php`-tiedostoon, PHP API:in, tietokantaan ja WordPressiin ei koskettu tässä päivityksessä. Vanha ZIP säilytetään palvelimella, kunnes token-korjauksen staging-koe on valmis.
- **Pakotetun token-päivityksen staging-tulos 24.8.2026:** käyttäjä kirjautui ulos, teki pakotetun selainpäivityksen ja kirjautui uudelleen samalla vahvistetulla Google-käyttäjällä uuden frontendin kautta. `/api/v1/admin/me` palautti edelleen `admin_forbidden`. Koska Firebase-käyttäjätietueen `emailVerified`, MariaDB:n sähköposti-, UID-, admin-rooli- ja aktiivisuusehdot sekä frontendin pakotettu token-päivitys on tarkistettu, uusia käyttäjä- tai tietokantamuutoksia ei tehdä ilman lähetetyn ID-tokenin väitteiden paikallista tarkistusta tai palvelimen rajattua staging-diagnostiikkaa.
- **Lähetetyn ID-tokenin paikallinen tarkistus 24.8.2026:** selaimen juuri `/api/v1/admin/me`-pyyntöön lähettämän tokenin payload purettiin paikallisesti PowerShellissä lähettämättä tokenia ulkopuoliseen palveluun tai päiväkirjaan. Tulos oli `email_present = false`, `email_verified = false` ja allekirjoitetun tokenin varattu `firebase.sign_in_provider = google.com`. Tämä selittää 403-vastauksen: käyttäjätietueen hallinnollinen `emailVerified`-päivitys ei tuottanut tokeniin valinnaisia sähköpostiväitteitä.
- **Rajattu palvelinkorjaus 24.8.2026:** Firebase-identiteettiin lisättiin allekirjoitetun tokenin varattu `sign_in_provider`-väite. Normaalitapauksessa API vaatii edelleen tokenin vahvistetun sähköpostin ja MariaDB-rivin sähköpostin vastaavuuden. Ainoa poikkeus on havaittu tapaus, jossa tokenin molemmat sähköpostiväitteet puuttuvat kokonaan: silloin hyväksytään vain `google.com`-providerin varmennettu UID, joka on ennalta provisioitu aktiiviseksi sallitun roolin `admin_users`-riviksi. Password-provider, puuttuva/väärä UID, passiivinen rivi ja väärä rooli hylätään edelleen. Selainprofiilin sähköpostia ei käytetä palvelinoikeuteen. Toteutukseen lisättiin hyväksyvä Google/UID-testi ja hylkäävä password-provider-testi; paikallista PHP-testiajoa ei ole vielä saatu suoritettua puuttuvan PHP-komentorivityökalun vuoksi.
- **Palvelinkorjauksen paketti 24.8.2026:** TypeScript-tarkistus, staging-build ja `git diff --check` läpäistiin. Uudelleenrakennetun staging-ZIPin SHA-256 on `cd62ed22328ce75bb110c52e998eca3167c02715460a01cc26b665e777dd14ee`. Selainpohjaisessa tiedostonhallinnassa päivitys rajataan kolmeen palvelimen `/website.aloitussivu-staging/src`-tiedostoon: `FirebaseIdentity.php`, `FirebaseIdTokenVerifier.php` ja `AdminAuthenticator.php`. Julkista frontendia, tietokantaa tai `secrets/config.php`-tiedostoa ei tarvitse muuttaa tässä vaiheessa uudelleen.
- **Kirjautumisarkkitehtuurin päätös 24.8.2026:** yhteistä palvelimen käyttäjätunnus–salasana-kirjautumista arvioitiin Firebase-vaihtoehdoksi. Sitä ei toteuteta kesken REL-07:n, koska turvallinen istunto-, CSRF-, kirjautumisrajoitus- ja auditointitoteutus kasvattaisi työpakettia vähintään työpäivällä ja yhteinen tunnus heikentäisi henkilökohtaista jäljitettävyyttä. REL-07 viimeistellään rajatulla Firebase/Google-UID-korjauksella. Palvelimen oma kirjautuminen voidaan suunnitella erilliseksi myöhemmäksi paketiksi ilman tunnusten tai salasanatiivisteiden tallentamista Gitiin.
- **PHP-korjauksen palvelinasennus 24.8.2026:** `FirebaseIdentity.php`, `FirebaseIdTokenVerifier.php` ja `AdminAuthenticator.php` ladattiin Cloudcityn selainpohjaisella tiedostonhallinnalla palvelimen `/website.aloitussivu-staging/src`-hakemistoon ja vanhat versiot korvattiin. Asennuksen jälkeinen `/api/v1/health` palautti `status: ok` ja `database: up`, joten PHP-muutos latautuu ilman näkyvää syntaksi- tai tietokantayhteysvirhettä. Varsinainen hyväksytyn ylläpitäjän `/api/v1/admin/me`-koe tehdään seuraavaksi.
- **Rajauksen jälkeinen 403 ja staging-diagnostiikka 24.8.2026:** health-kokeen jälkeen sama hyväksytty Google-käyttäjä sai edelleen `admin_forbidden`-vastauksen. Seuraavaa koetta varten 403-vastaukseen lisättiin vain staging-ympäristössä tarkistusten totuusarvot: löytyikö UID-rivi, oliko se aktiivinen ja sallittu rooli, olivatko tokenin sähköpostiväitteet läsnä ja vastaavia sekä tunnistuiko allekirjoitettu Google-providerin UID-poikkeus. Diagnostiikka ei palauta UID:tä, sähköpostia, roolin nimeä, tokenia tai tietokannan arvoja ja pysyy tuotannossa pois käytöstä.
- **Staging-diagnostiikan tulos ja UID-linjaus 24.8.2026:** `/api/v1/admin/me` vahvisti, että allekirjoitetun tokenin UID:llä löytyi aktiivinen sallitun roolin tietokantarivi (`row_found`, `active` ja `role_allowed` olivat tosia), mutta tokenista puuttuivat sekä sähköpostiväitteet että palvelimen odottama `firebase.sign_in_provider`. Oikeuspäätös muutettiin Firebase-standardin mukaiseen ensisijaiseen tunnisteeseen: varmennettu `sub`-UID, aktiivinen `admin_users`-rivi ja sallittu rooli ratkaisevat oikeuden. Sähköposti- ja provider-väitteet ovat valinnaista diagnostiikkaa, eikä selaimen profiilitietoja käytetä oikeuden myöntämiseen. Väärä tai puuttuva UID, passiivinen rivi ja väärä rooli hylätään edelleen.
- **Hyväksytyn ylläpitäjän staging-koe 24.8.2026:** UID-pohjainen oikeustarkistus vietiin palvelimelle yhden `AdminAuthenticator.php`-tiedoston päivityksenä. Asennuksen jälkeen health palautti edelleen `status: ok` ja `database: up`, ja yksi ennalta hyväksytty Google/Firebase-ylläpitäjä pääsi stagingin ylläpitoon. Tulos vahvistaa allekirjoituksen, projektin, UID:n, aktiivisuuden ja roolin koko tunnistusketjun toimivaksi. Toisen hyväksytyn ylläpitäjän sekä käytöstä poistetun oikeuden kokeet ovat vielä avoinna.
- **Toisen ylläpitäjän ensikoe 24.8.2026:** toinen tarkoitettu Google-ylläpitotunnus sai UID-pohjaisen korjauksen jälkeen edelleen `admin_forbidden`-vastauksen. Tunnisteita ei kirjattu päiväkirjaan eikä Firebase-käyttäjää tai tietokantariviä muutettu. Seuraava rajaus on saman pyynnön staging-diagnostiikan totuusarvot, joilla erotetaan puuttuva tietokantarivi passiivisesta tai vääräroolisesta rivistä ennen mitään ympäristömuutosta.
- **Toisen ylläpitäjän diagnostiikka 24.8.2026:** staging-vastaus palautti toiselle tunnukselle `row_found = false`; samalla aktiivisuus, sallittu rooli ja `uid_authorized` olivat epätosia, koska UID-riviä ei löytynyt. Ensimmäisen tunnuksen toimiva koe vahvistaa palvelinkorjauksen, joten toisen tunnuksen ongelma rajautuu ympäristökohtaiseen provisiointiin. Seuraavaksi verrataan toisen hyväksytyn Google-käyttäjän UID:tä saman sähköpostin tietokantariviin vain koontituloksena; riviä tai Firebase-käyttäjää ei muuteta ennen kaksoiskäyttäjän tunnistamista.
- **Toisen ylläpitäjän provisiointikorjaus 24.8.2026:** Firebase Consolessa vahvistettiin oikea projekti, sama hyväksytty sähköposti ja erilliset Google- sekä email/password-providerin käyttäjät. Tietokannan aktiivinen `admin`-rivi osoitti vanhaan Google-käyttäjätietueeseen, ei selaimen nykyisen Google-kirjautumisen UID:hen. Rivi päivitettiin nykyisen G-provider-käyttäjän UID:hen kohdistetulla staging-SQL:llä. Tämän jälkeen myös toinen ennalta hyväksytty ylläpitäjä pääsi stagingin ylläpitoon. Molempien hyväksyttyjen tunnusten positiivinen koe on valmis; provider-kaksoiskäyttäjien UID:itä tai sähköposteja ei kirjattu dokumentaatioon.
- **Oikeuden poistamisen ja palautuksen koe 24.8.2026:** toisen toimivaksi vahvistetun ylläpitäjän staging-rivin `active` asetettiin väliaikaisesti arvoon `0` toisen ylläpitäjän jäädessä aktiiviseksi. Jo kirjautuneen selaimen seuraava ylläpitopyyntö palautti odotetusti `admin_forbidden`-eston. Sama rivi palautettiin arvoon `active = 1`, tietokantatarkistus vahvisti UID-rivin, aktiivisuuden ja `admin`-roolin, ja selaimen pääsy palautui heti. Positiivinen ja negatiivinen staging-oikeuskoe ovat tältä osin valmiit.
- **Provider-kaksoiskäyttäjien rajaus 24.8.2026:** kummankin hyväksytyn sähköpostin alla oli Firebase Authenticationissa erillinen Google-providerin ja email/password-providerin käyttäjätietue. Molempien password-providerin UID:ien `admin_users`-osumaksi vahvistettiin `0`, ja kooditarkistus osoitti sovelluksen käyttävän vain Google-popup-kirjautumista. Nämä kaksi ylimääräistä password-käyttäjää disabloitiin Firebase Consolessa; niitä ei poistettu, eikä kumpaankaan toimivaan G-providerin ylläpitäjään tehty muutosta.
- **Runtime-hakemistojen oikeudet 24.8.2026:** palvelimen yksityisten `src`, `secrets`, `logs`, `cache` ja `protected_uploads` -hakemistojen oikeuksiksi asetettiin Cloudcityn tiedostonhallinnassa `750`. Julkisen `public_html`-hakemiston oikeuksia ei muutettu ja yksityisen `secrets/config.php`-tiedoston oikeus säilyi arvossa `640`.
- **Stagingin Basic Auth -tunnisteen kierrätys 24.8.2026:** HTTP-otsaketarkistuksessa selaimesta kopioitiin erehdyksessä vastausotsikoiden sijasta pyyntöotsikot, jolloin Basic Auth -tunniste tuli näkyviin työistunnossa. Tunniste käsiteltiin paljastuneena ja stagingin salasanasuojauksen salasana vaihdettiin heti. Uutta käyttäjätunnusta, salasanaa tai Authorization-arvoa ei kirjattu repositorioon tai dokumentaatioon.
- **HTML-vastausotsikot 24.8.2026:** uuden Basic Auth -tunnisteen toiminta varmennettiin erillisellä selainkokeella ja tunnisteen arvoa paljastamattomalla curl-HEAD-pyynnöllä. `index.html` palautti 200 sekä odotetut HSTS-, CSP-, `X-Content-Type-Options`, `Referrer-Policy`, `X-Frame-Options`, `Permissions-Policy`- ja stagingin `X-Robots-Tag`-otsikot. HTML:n `Cache-Control` oli `no-cache, max-age=0, must-revalidate`. HEAD-vastauksesta puuttuvaa sisällön pakkausotsikkoa ei tulkita pakkaustestiksi, koska vastauksessa ei siirretty runkoa.
- **API-välimuistiotsikot 24.8.2026:** rungollinen `GET /api/v1/health` palautti 200 ja JSON-sisältötyypin. API:n `.htaccess`-rajaus näkyi vastauksessa arvoina `Cache-Control: no-store`, `Pragma: no-cache` ja `Expires: 0`; lisäksi suojaus- ja stagingin noindex-otsikot säilyivät mukana. API:n no-store-koe on valmis.
- **Staattisen resurssin välimuisti ja pakkaus 24.8.2026:** stagingin hashatulle JavaScript-resurssille tehty rungollinen curl-pyyntö palautti 200, `Content-Encoding: gzip`, `Vary: Accept-Encoding` ja `Cache-Control: public, max-age=31536000, immutable`. HTML:n uudelleenvalidointi, API:n no-store sekä staattisten resurssien pitkä välimuisti ja pakkaus on siten varmennettu oikeasta Cloudcity-ympäristöstä.
- **WordPress-savukoe muutoksen jälkeen 24.8.2026:** julkinen lukukoe toistettiin samoilla vertailukohteilla. Etusivu, Etäopastus, Ajankohtaista, Yhteystiedot ja `?s=digiopastus` palauttivat 200 samoihin lopullisiin osoitteisiin; sivujen otsikot vastasivat lähtötasoa ja hakusivu sisälsi `Hakutulokset`-merkin. `/wp-admin/` palautti edelleen 302-ohjauksen WordPressin kirjautumissivulle, joka palautti 200. Sovittu media palautti 200, `image/jpeg` ja 152080 tavua. WordPressin tiedostoihin, tietokantaan, teemaan, lisäosiin tai asetuksiin ei tehty muutoksia, eikä ennen/jälkeen-savukokeessa havaittu regressiota.
- **Nykykoodin staging-paketti 24.8.2026:** UID-pohjaisen ylläpito-oikeuskorjauksen jälkeen ajettiin `npx tsc --noEmit`, `npm run check:secrets`, `npm run build:staging`, paketointiskripti ja `git diff --check`; tarkistukset läpäisivät ja salaisuusskannaus ei löytänyt kovakoodattuja avaimia. Uusi `C:\dev\Aloitussivu\.tmp\aloitussivu-rel07-staging.zip` sisältää 109 merkintää ja 11 noindex-HTML-tiedostoa, mukana ovat nykyinen `AdminAuthenticator.php` sekä vain `config.staging.example.php`; oikeaa `secrets/config.php`-tiedostoa ei ole mukana. ZIPin SHA-256 on `1577b6f0f796322b704363439384d5857cfb80e0a143f66c303adb68be587b60`. PHP-komentorivityökalu ei ole tässä työasemaistunnossa saatavilla, joten muuttuneiden PHP-tiedostojen paikallinen lint- ja sopimustestiajo jää palvelinhealthin ja staging-integraatiokokeiden rinnalla vielä avoimeksi testinäytöksi.
- **REL-07-resurssien ennakkolataus 24.8.2026:** nykykoodin paketin 50 hashattua asset-tiedostoa ladattiin palvelimen `public_html/assets`-hakemistoon ennen REL-06:n muodollista sulkemista. Nykyisiä HTML-tiedostoja ei tässä vaiheessa korvattu, joten uudet erinimiset resurssit ovat vain rinnalla eivätkä muuta selaimen lataamaa versiota. Muu REL-07-tiedostosynkronointi pidetään tauolla oikean pakettijärjestyksen palauttamiseksi.
- **PHP 8.4.24 -testinäyttö 24.8.2026:** työtilan `.tmp`-hakemistoon ladattiin virallisesta PHP for Windows -jakelusta kannettava PHP 8.4.24 NTS x64 muuttamatta järjestelmän PATHia tai Windows-asennusta. Kaikki 37 API:n PHP-tiedostoa läpäisivät lintin. Ensimmäinen sopimustestiajo sai 32/33 tuloksen, koska kannettava ajopaketti ei löytänyt RSA-testiavaimen luontiin omaa `openssl.cnf`-tiedostoaan. Kun `OPENSSL_CONF` osoitettiin vain testiprosessin ajaksi paketin omaan konfiguraatioon, kaikki 33/33 testiä läpäisivät. Mukana läpäisivät allekirjoitetun Firebase-tokenin varmennus, puuttuvien valinnaisten identiteettiväitteiden UID-oikeus, tuntemattoman ja passiivisen UID:n esto sekä stagingin erillinen Firebase-token-otsake.
- **REL-07:n julkisten tiedostojen lopullinen synkronointi 24.8.2026:** nykyisen staging-paketin kaikki 18 `public_html`-hakemiston ylätason tiedostoa ladattiin Cloudcityyn ja samannimiset tiedostot korvattiin. Ensimmäinen latausyritys oli valinnut paikallisen vanhemman version, mikä todettiin palvelimen `yllapito.html`-sisällön vanhoista resurssinimistä. Lähdetiedostot koottiin tämän jälkeen erilliseen 18 tiedoston latauskansioon, oikea `yllapito.html` varmennettiin ensin sekä Cloudcityn editorista että selaimen lähdekoodista ja kaikki 18 tiedostoa ladattiin siitä uudelleen. Palvelimen lähdekoodi viittaa nyt paketin nykyisiin hashattuihin `admin`- ja `index`-resursseihin. Tietokantaan, yksityiseen konfiguraatioon ja WordPressiin ei koskettu.
- **Ylläpidon lopullinen frontend-savukoe 24.8.2026:** kumpikin ennalta hyväksytty Google-ylläpitotunnus kirjautui onnistuneesti nykyisellä REL-07-frontendilla ja avasi ylläpitonäkymän ilman oikeusvirhettä. Aiemmin toisessa selaimessa näkynyt vanhentunut GitHub Pages -ohje paikannettiin vanhaan JavaScript-versioon; oikean HTML-version lataamisen jälkeen myös toisen tunnuksen uloskirjautuminen palautti kirjautumisnäkymän ilman Firebase-virhettä. Molempien tunnusten positiivinen loppukoe on valmis.
- **Julkinen loppusavukoe 24.8.2026:** lopullisen 18 tiedoston synkronoinnin jälkeen stagingin etusivu, linkkisivu ja testipalautelomake avautuivat normaalisti, tarkoituksella puuttuva osoite näytti oman 404-sivun ja `/api/v1/health` palautti edelleen `status: ok` sekä `database: up`. Nykyisen frontendin, PHP-entrypointin ja MariaDB-yhteyden nopea uusintakoe läpäistiin. Palvelimelle aiemmin ladatun staging-ZIPin täsmällinen tunnistaminen ja poistaminen on viimeinen jäljellä oleva ympäristösiivous.
- **Palvelin-ZIPin loppuselvitys 24.8.2026:** Cloudcityn tiedostonhallinnasta tarkistettiin sekä `/website.aloitussivu-staging/` että sen `public_html`-hakemisto lopullisten savukokeiden jälkeen. Kummassakaan ei ollut `.zip`-tiedostoa, joten poistettavaa palvelinpakettia ei ollut eikä tiedostoja poistettu. Paikallinen yksilöity staging-ZIP säilyy työtilan `.tmp`-hakemistossa testinäyttönä.
- **Seuraavalle paketille luovutettu:** REL-08 voi jatkaa hyväksytystä Cloudcity-stagingistä. TEST-P2:n uudet tulokset ja havaintolistan lukitus käsitellään erikseen REL-10:ssä.
- **Tila:** valmis

## Paketti: REL-08

- **Päivämäärä:** 24.8.2026 (ennakkotyö; suunniteltu pakettipäivä 26.8.2026)
- **Vastuuhenkilö:** Eero Tuomenoksa
- **Tietosuojatarkistaja:** nimetään ennen oikean Firestore-viennin käsittelyä
- **Riippuvuudet tarkistettu:** kyllä; REL-02, REL-06 ja REL-07 ovat valmiit. Työ rajataan Firestore-vientiin ja Aloitussivun staging-tietokantaan. Tuotantoa ja WordPressiä ei muuteta.
- **Paikallinen migraatiotyökalu:** lisättiin vain luku -Admin SDK -vienti kymmenelle dokumentoidulle kokoelmalle, joista yhdeksän muunnetaan P1-skeemaan ja poistuvan toiminnon `adminStats` säilytetään vain arkistona. Erillinen muunnos muodostaa alkuperäisillä tunnisteilla idempotentin MariaDB-SQL:n, lukumäärä- ja tiivistepohjaisen tarkistusraportin sekä suojattuun hakemistorakenteeseen purettavat liitteet.
- **Tietosuojarajaus:** Admin SDK -avain, Firestore-vienti, palautesisältöjä sisältävä SQL, liitteet ja poistuvan toiminnon arkisto on pidettävä repositorion ja synkronoitujen kansioiden ulkopuolella. Skriptit estävät syöte- ja tulospolut työtilan sisällä. Konsoliin tulostetaan vain projektitunnus, ajat, kokoelma- ja taulurivimäärät, liitemäärä sekä poikkeamien lukumäärä. Poikkeamaraportti käyttää dokumenttitunnisteista vain SHA-256-tiivisteen lyhyttä osaa.
- **Kenttä- ja liitemuunnokset:** camelCase-kentät muunnetaan nykyisen 001/002-skeeman snake_case-kentiksi, aikaleimat UTC-`DATETIME(6)`-arvoiksi, URL:t palvelimen normalisointia vastaavaan muotoon ja sisäkkäiset käyttötilastot päivä-, sivu- ja linkkiriveiksi. Liitteiltä tarkistetaan sallittu MIME-tyyppi, 450 KiB:n raja, Base64-rakenne, ilmoitetun ja todellisen koon vastaavuus sekä PNG/JPEG/WebP/GIF-tiedostoallekirjoitus. Tallennusavaimet täyttävät nykyisen suojatun palvelintallennuksen polkusäännön.
- **Idempotenssimalli:** jokainen sisältötaulu käyttää alkuperäistä Firestore-tunnistetta ja `INSERT ... ON DUPLICATE KEY UPDATE` -tuontia. Staging-hyväksyntä vaatii saman SQL:n kaksi peräkkäistä tuontia sekä muuttumattomat rivimäärät toisella ajolla; paikallinen fixture-testi varmistaa kaikkien 11 kohdetaulun upsert-rakenteen.
- **Paikalliset testit 24.8.2026:** `npm run rel08:test` läpäisi kelvollisen täyden tietoryhmäfixturen, URL-normalisoinnin, liitteen suojatun polun, idempotenttien lauseiden muodostuksen, henkilötiedottoman tarkistus-SQL:n ja virheellisen kuvatiedoston eston. Node-syntaksitarkistukset läpäistiin kaikille kolmelle työkalumoduulille. `npm run check:secrets` ja `git diff --check` läpäistiin; testikuvan ensimmäinen Base64-fixture aiheutti salaisuusskannauksen väärän positiivisen osuman ja korvattiin binäärisesti muodostettavalla testidatalla.
- **Ohje ja delta-käsikirjoitus:** turvallinen vienti-, muunnos-, varmistus-, kahden tuonnin, täsmäytyksen, yksityisyyskokeen ja palautuksen järjestys on dokumentoitu tiedostossa `docs/rel08-firestore-migraatio-ohje.md`. Alustava tuotannon kirjoituskaton enimmäiskesto on 25 minuuttia. Delta perustuu luonti- ja päivitysaikoihin; koska Firestore ei tarjoa poistojen aikaleimaa, lopullinen vaihto vaatii kirjoituskatkon ja koko tunnistejoukon täsmäytyksen.
- **Täysi Firestore-vienti 24.8.2026:** oikean `aloitussivu-5d50c`-projektin projektitunnus tarkistettiin avaintiedoston metatiedosta ennen verkkoyhteyttä ja vain luku -vienti valmistui klo 09.53.31 UTC. Vienti sisälsi 230 dokumenttia: `linkReports` 1, `feedbackItems` 3, `feedbackAttachments` 0, `testFeedbackResponses` 4, `approvedLinks` 0, `blockedLinks` 0, `scamAlerts` 32, `ncscScrapeLog` 144, `usageStats` 45 ja vain arkistoitava `adminStats` 1. Admin SDK -avainta, tiedostopolkua tai dokumenttien sisältöä ei tulostettu eikä tallennettu repositorioon.
- **Muunnos- ja poikkeamatulos 24.8.2026:** viennin sisältötiiviste on `25f3e405c7a0166b0a561b1550dd498bb7b5894451810b9c928972a8e1890df4`. Muunnos tuotti `link_reports` 1, `feedback_items` 3, `feedback_attachments` 0, `test_feedback_responses` 4, `approved_links` 0, `blocked_links` 0, `scam_alerts` 32, `ncsc_scrape_logs` 144, `usage_daily` 45, `usage_page_daily` 111 ja `usage_link_daily` 205 riviä. Poikkeamia oli 0 ja liitetiedostoja 0. Yksityinen SQL, vienti ja `adminStats`-arkisto säilyvät vain repositorion ulkopuolisessa paikallisessa kansiossa.
- **Staging-varmuuskopio 24.8.2026:** tuore REL-08:aa edeltävä phpMyAdmin-vienti tallennettiin repositorion ulkopuoliseen yksityiseen kansioon. Tiedoston olemassaolo, järkevä koko ja tuore aikaleima varmistettiin avaamatta tai tulostamatta tietokannan sisältöä.
- **Stagingin lähtörivit 24.8.2026:** ennen tuontia `link_reports`, `feedback_items`, `feedback_attachments`, `test_feedback_responses`, `approved_links`, `blocked_links`, `scam_alerts` ja `ncsc_scrape_logs` sisälsivät kukin 0 riviä. Käyttötilastojen lähtömäärät olivat `usage_daily` 2, `usage_page_daily` 7 ja `usage_link_daily` 4. Tarkistus oli vain luku -kysely.
- **Ensimmäisen tuontiyrityksen korjaus 24.8.2026:** ensimmäinen SQL-ajo pysähtyi ennen `COMMIT`-lausetta, koska generaattori muodosti tyhjästä ei-nullattavasta tekstikentästä MariaDB:lle virheellisen heksaliteraalin. Generaattori korjattiin käyttämään kelvollista tyhjää UTF-8-lauseketta ja lisättiin regressiotesti, joka sekä vaatii oikean muodon että estää virheellisen muodon. `npm run rel08:test`, salaisuustarkistus ja Node-syntaksitarkistukset läpäisivät. Tuonti rakennettiin uudelleen erilliseen `-02`-hakemistoon samasta tarkistetusta Firestore-viennistä; poikkeamia oli edelleen 0. Ennen uutta tuontia varmistetaan vain luku -kyselyllä, että kesken jäänyt transaktio palautui lähtötilaan.
- **Kesken jääneen transaktion varmistus 24.8.2026:** kaikki 11 taulukohtaista rivimäärää tarkistettiin uudelleen. Ne olivat täsmälleen samat kuin ennen epäonnistunutta ajoa: kahdeksan sisältötaulua 0 riviä ja käyttötilastotaulut 2, 7 ja 4 riviä. Epäonnistunut tuontiyritys ei muuttanut staging-tietokantaa.
- **Ensimmäinen korjattu staging-tuonti 24.8.2026:** yksityisen `-02`-paketin `import.sql` suoritettiin `dbtma_aloitussivu`-staging-tietokantaan onnistuneesti. phpMyAdmin suoritti 549 kyselyä, mikä vastaa 545 idempotenttia tietorivilauseketta sekä neljää transaktion ja istunnon ohjauslausetta. Tuotantoon tai WordPressiin ei koskettu.
- **Ensimmäisen tuonnin rivitäsmäytys 24.8.2026:** tuonnin jälkeen rivimäärät olivat `link_reports` 1, `feedback_items` 3, `feedback_attachments` 0, `test_feedback_responses` 4, `approved_links` 0, `blocked_links` 0, `scam_alerts` 32, `ncsc_scrape_logs` 144, `usage_daily` 45, `usage_page_daily` 115 ja `usage_link_daily` 209. Kahdeksan sisältötaulua täsmäsivät vientiin. Käyttötilastoissa kaksi päiväavainta ja kolme sivuavainta osuivat lähtöaineiston olemassa oleviin riveihin ja päivittyivät, kun taas neljä aiempaa linkkiriviä säilyi uusien rinnalla; tulos vastaa lähtö- ja vientimäärien avainkohtaista yhdistymistä.
- **Idempotenssikoe 24.8.2026:** sama korjattu `-02`-tuonti ajettiin uudelleen. Kaikki 11 rivimäärää pysyivät samoina: 1, 3, 0, 4, 0, 0, 32, 144, 45, 115 ja 209 taulujärjestyksessä. Tuonti voidaan ajaa uudelleen ilman kaksoisrivejä; idempotenssivaatimus täyttyy.
- **Henkilötiedoton täsmäytys 24.8.2026:** `verify.sql` suoritettiin stagingissä onnistuneesti. Kaikki 11 rivimäärää, kahdeksan aikaväliä ja kaikki 23 odotettua pistokoetiivistettä vastasivat paikallista `reconciliation-report.json`-raporttia. Tyhjien `approved_links`- ja `blocked_links`-taulujen aikavälit olivat odotetusti `NULL`. phpMyAdminin alun tyhjä tulosjoukko syntyi vain istunnon UTC-aikavyöhykkeen asettavasta `SET`-komennosta eikä ollut virhe. Yksityistä sisältöä ei kirjattu päiväkirjaan.
- **Yksityisyyskokeen ensimmäinen negatiivinen tarkistus 24.8.2026:** stagingin Cloudcity-kansiosuojaus hyväksyi oman kansiotunnuksensa etusivulle, mutta kirjautumattoman suora pyyntö ylläpidon palautelistaan ei näyttänyt sisältöä. API:n 401-vastaus yhdistettynä palvelimen Basic Auth -haasteotsakkeeseen näkyi selaimessa uutena salasanakyselynä; saman kansiosalasanan uudelleensyöttö ei voinut ohittaa Firebase-tokenin puuttumista. Palautelistan suora kirjautumaton luku on estetty. Testipalautereitin, liitepolun ja hyväksytyn ylläpitäjän positiiviset tarkistukset ovat vielä avoinna.
- **Hyväksytyn ylläpitäjän positiivinen avaus 24.8.2026:** saman selainistunnon varsinainen `yllapito.html` avautui onnistuneesti Cloudcity-kansiosuojauksen ja hyväksytyn Google-kirjautumisen jälkeen. Seuraavaksi varmistetaan, että ylläpitäjä näkee siirrettyjen palautteiden ja testipalautteiden odotetut lukumäärät ilman sisällön kopioimista testiraporttiin.
- **Siirretyn yksityisen aineiston positiivinen lukukoe 24.8.2026:** hyväksytyllä Google-ylläpitäjällä Cloudcity-providerin kehitysjono näytti odotetut 3 palautetta ja testipalautteen koonti odotetut 4 vastausta. Sisältöä ei kopioitu testiraporttiin. Positiivinen MariaDB-luku toimii molemmissa yksityisissä näkymissä.
- **Kirjautumattomien yksityisten näkymien koe 24.8.2026:** Google-ylläpidosta uloskirjautumisen jälkeen sekä kehitysjono että testipalautteen koonti näyttivät vain ylläpitäjän kirjautumisvaatimuksen eivätkä listoja, lukumääriä tai yksittäisiä vastauksia. Viennissä ei ollut liitteitä, joten siirrettävän liitetiedoston positiivista avausta ei tarvittu; suora ylläpidon palautereitti oli jo erikseen estetty kirjautumattomalta. REL-08:n yksityisyysvaatimus täyttyy tämän aineiston osalta.
- **Delta-vienti ja muunnos 24.8.2026:** alkuperäisen täysviennin aikarajasta tehty vain luku -delta-vienti valmistui 20,06 sekunnissa ja muunnos 4,35 sekunnissa. Delta sisälsi 0 palautetta, ilmoitusta, liitettä, testipalautetta, hyväksyttyä tai estettyä linkkiä, huijausvaroitusta ja NCSC-ajolokia sekä 1 muuttuneen `usageStats`-dokumentin ja 1 vain arkistoitavan `adminStats`-dokumentin. MariaDB-delta muodostui 1 `usage_daily`-, 3 `usage_page_daily`- ja 4 `usage_link_daily`-upsertista. Poikkeamia ja liitteitä oli 0. Delta-viennin sisältötiiviste on `8f77479d31a79af4bb1fa9e4dc04f37165dcae4a042d10ee45d2ed2813dec625`; yksityiset tiedostot säilyvät repositorion ulkopuolella.
- **Ensimmäinen delta-tuonti stagingiin 24.8.2026:** delta-paketin `import.sql` suoritettiin onnistuneesti `dbtma_aloitussivu`-tietokantaan. phpMyAdmin suoritti odotetut 12 kyselyä: kahdeksan käyttötilastojen upsertia ja neljä istunnon tai transaktion ohjauslausetta. Varoituksia tai virheitä ei ollut. Upsertit kohdistuivat olemassa oleviin avaimiin; phpMyAdminin päivityksistä ilmoittama kahden affected-row-arvo ei tarkoita kahta uutta riviä.
- **Delta-idempotenssin uusinta-ajo 24.8.2026:** sama delta-SQL suoritettiin toisen kerran. phpMyAdmin suoritti jälleen odotetut 12 kyselyä ilman MySQL-virhettä. Ajotason idempotenssi läpäisi; lopullinen rivimäärien jälkilaskenta tehdään vielä erikseen.
- **Deltan lopputäsmäytys 24.8.2026:** toisen delta-ajon jälkeen kaikki 11 rivimäärää pysyivät odotettuina: `link_reports` 1, `feedback_items` 3, `feedback_attachments` 0, `test_feedback_responses` 4, `approved_links` 0, `blocked_links` 0, `scam_alerts` 32, `ncsc_scrape_logs` 144, `usage_daily` 45, `usage_page_daily` 115 ja `usage_link_daily` 209. Delta ei luonut kaksoisrivejä. Täysi vienti, kaksi täystuontia, henkilötiedoton täsmäytys, yksityisyyskokeet, delta-vienti, kaksi delta-tuontia ja deltan jälkitäsmäytys ovat teknisesti valmiit.
- **Avaimen turvallisuussiivous 25.8.2026:** käyttäjä vahvisti väliaikaisen REL-08 Admin SDK -avaimen poistamisen Google Cloudista. Paikallisen tiedoston ratkaistu polku, yksityisen juurihakemiston sisällä pysyminen, täsmällinen tiedostonimi ja olemassaolo tarkistettiin ennen poistoa. Vain avaintiedosto poistettiin; täysi vienti, delta-vienti ja staging-varmuuskopio säilyvät yksityisinä REL-09-todisteina. Poiston jälkeen avaintiedostoa ei enää löytynyt.
- **Loppuvarmistukset 25.8.2026:** `npm run rel08:test`, `npm run check:secrets` ja kaikkien kolmen REL-08-Node-työkalun syntaksitarkistukset läpäisivät. `git diff --check` ei löytänyt sisältövirheitä; tulosteessa oli vain työpuun ennestään tunnettuja LF/CRLF-varoituksia.
- **Avoimet P1/P2/P3-havainnot:** ei avoimia REL-08:n data-, tuonti-, yksityisyys- tai avainesteitä. Aineistossa ei ollut siirrettäviä liitteitä.
- **Seuraavalle paketille luovutettu:** REL-09 saa hyväksytyn ja täsmäytetyn Cloudcity-stagingin, yksityisessä paikallishakemistossa säilytettävän staging-varmuuskopion sekä täys- ja delta-ajojen henkilötiedottomat todisteet. Tuotantoon ja WordPressiin ei koskettu REL-08:ssa.
- **Tila:** valmis

## Paketti: REL-09

- **Päivämäärä:** 25.8.2026 (ennakkotyö; suunniteltu pakettipäivä 27.8.2026)
- **Omistaja:** Eero Tuomenoksa / kehitys + Fakiirimedia
- **Tarkistaja:** avoin
- **Riippuvuudet tarkistettu:** kyllä; REL-07 ja REL-08 ovat valmiit. Stagingissä on täsmäytetty MariaDB-aineisto, ja REL-08:n yksityinen varmistus säilyy repositorion ulkopuolella. Tuotantoa ja WordPressiä ei muuteta.
- **Käyttötilaston palvelinpolku:** frontendin `pageview`- ja `linkClick`-tapahtumat käyttävät Cloudcity-providerissa saman originin `POST /api/v1/usage-events` -reittiä. API päivittää Europe/Helsinki-päivään sidotut `usage_daily`-, `usage_page_daily`- ja `usage_link_daily`-aggregaatit eikä tallenna käyttötilastoon raakaa IP-osoitetta, käyttäjätunnistetta, evästettä, user agentia tai selaimen sormenjälkeä.
- **NCSC-tausta-ajo:** lisättiin PHP 8.4 -palvelinajo, jota käyttävät sekä web-juuren ulkopuolinen CLI-skripti että kirjoittavan ylläpitoroolin `POST /api/v1/admin/ncsc-run` -käsiajo. Ajo hyväksyy lähteiksi vain Kyberturvallisuuskeskuksen HTTPS-osoitteet, rajaa vastekoon ja aikakatkaisut sekä tuottaa varoitustekstin ilman ulkopuolista tekoälyavainta.
- **Lukitus ja idempotenssi:** MariaDB:n yhteyskohtainen `GET_LOCK` estää päällekkäiset ajot. Onnistunut lähde ohitetaan kuuden päivän ajan. Uuden varoituksen tunniste muodostetaan deterministisesti lähdeosoitteesta ja alkuperäisestä otsikosta, ja tietokantakirjoitus käyttää upsertia; käsiajon tuloksesta kirjoitetaan lisäksi sisältöä paljastamaton auditointimerkintä.
- **Ajoloki ja virherajaus:** onnistumiset, ohitukset, tyhjä syöte ja virheet tallennetaan nykyiseen `ncsc_scrape_logs`-tauluun. Lokeihin sallitaan vain tunnettu tekninen virhekoodi; tuntematon poikkeus korvataan yleisellä `ncsc_job_failed`-koodilla. CLI-yhteenveto näyttää turvallisesti myös UTC- ja Europe/Helsinki-ajan aikavyöhykekokeen tueksi.
- **Paikalliset testit 25.8.2026:** kaikki API:n PHP-tiedostot läpäisivät syntaksitarkistuksen. PHP 8.4.24:n OpenSSL-, PDO SQLite- ja mbstring-laajennuksilla koko 39 testin sopimussarja läpäisi ilman virheitä. Uudet testit kattavat RSS- ja 2026-sivurakenteen parsinnan, lähdeosoitteiden rajauksen, päällekkäisen ajon eston, kuuden päivän uusintaeston, deterministisen duplikaattisuojan, lukon vapautuksen, turvallisen virhelokin sekä ylläpidon käsiajon ja auditoinnin. `npx tsc --noEmit` läpäisi.
- **Lukevan oikean lähteen koe 25.8.2026:** Kyberturvallisuuskeskuksen oikea RSS-syöte haettiin paikallisesta PHP-ajosta ilman tietokantakirjoitusta tai artikkelisisällön tulostusta. Rajaus löysi kaksi kohdetta: yhden viikkokatsauksen ja yhden kuluttajalle olennaisen uutisen.
- **Käyttöönotto- ja palautusohje:** Cloudcityn selainpohjaisen tiedostonhallinnan ZIP-rajoitus, puretun paketin tiedostosiirto, cronin ajankohdat, turvalliset staging-kokeet, erillinen tietokantapalautus ja WordPressistä eristetty palautusharjoitus on dokumentoitu tiedostossa `docs/rel09-tausta-ajot-ja-palautuskoe.md`.
- **REL-09-staging-paketti 25.8.2026:** staging-build ja paketointi valmistuivat onnistuneesti. Puretussa paketissa on 117 tiedostoa, joista 18 on `public_html`-hakemiston ylätason tiedostoja, 50 hashattuja asset-tiedostoja ja 43 PHP-tiedostoja. Mukana ovat web-juuren ulkopuolinen `cron/ncsc.php`, kuusi `Ncsc`-luokkatiedostoa ja malliasetus, mutta ei oikeaa `secrets/config.php`-tiedostoa, `.env`-tiedostoa, palvelutiliavainta tai muuta kielletyksi rajattua tiedostoa. ZIPin SHA-256 on `80ceb3223e9906b567fe576248cd384d8fa0e98486b81c053e5ea0531c963810`. Selainpohjaista Cloudcity-hallintaa varten käytetään ensisijaisesti valmiiksi purettua kansiota `C:\dev\Aloitussivu\.tmp\rel09-staging-package`.
- **Loppuvarmistukset ennen stagingiä 25.8.2026:** `npx tsc --noEmit`, `npm run check:secrets`, `npm run build:staging`, PHP-lint, 39/39 PHP-sopimustestiä ja `git diff --check` läpäisivät. Diff-tarkistus tulosti vain työpuun ennestään tunnetut LF/CRLF-varoitukset eikä sisältövirheitä.
- **REL-09-varmistus ennen staging-päivitystä 25.8.2026:** käyttäjä vahvisti nykyisen `dbtma_aloitussivu`-staging-tietokannan uuden SQL-varmistuksen valmistuneeksi ennen REL-09-palvelintiedostojen lataamista. Varmistus säilytetään repositorion ulkopuolisessa yksityisessä kansiossa; tiedoston sisältöä tai yksityistä aineistoa ei kopioitu päiväkirjaan.
- **API-luokkien staging-synkronointi 25.8.2026:** puretun REL-09-paketin kaikki 39 `src`-hakemiston PHP-tiedostoa ladattiin Cloudcityn `/website.aloitussivu-staging/src/`-hakemistoon ja samannimiset tiedostot korvattiin. `secrets`-hakemistoon, tuotantoon tai WordPressiin ei koskettu. Health-uusintakoe tehdään ennen cron-skriptin lisäämistä.
- **API-health synkronoinnin jälkeen 25.8.2026:** stagingin `GET /api/v1/health` palautti 200-tasoisen JSON-vastauksen, jossa sovelluksen tila oli `ok`, tietokanta `up` ja versio `v1`. Uudet lähdetiedostot latautuvat ja nykyinen staging-MariaDB-yhteys säilyi toimivana. Pyyntötunnistetta ei kirjattu päiväkirjaan.
- **Cron-skriptin staging-sijoittelu 25.8.2026:** `cron`-hakemisto luotiin stagingin ympäristöjuureen web-juuren ulkopuolelle ja puretun REL-09-paketin `ncsc.php` ladattiin siihen. Hakemiston oikeudeksi asetettiin `750` ja skriptin oikeudeksi `640`. Skriptiä ei tässä vaiheessa vielä ajastettu tai suoritettu, eikä tuotantoon tai WordPressiin koskettu.
- **REL-09-resurssien esilataus 25.8.2026:** puretun paketin kaikki 50 `public_html/assets`-tiedostoa ladattiin stagingin vastaavaan asset-hakemistoon ja samannimiset tiedostot korvattiin. Ylätason HTML-tiedostoja ei vielä tässä vaiheessa vaihdettu, joten uudet hashatut resurssit olivat vain rinnalla ennen hallittua frontend-version vaihtoa.
- **REL-09-frontendin vaihto 25.8.2026:** paketin kaikki 18 `public_html`-hakemiston ylätason tiedostoa, mukaan lukien stagingin `.htaccess`, ladattiin palvelimelle ja samannimiset tiedostot korvattiin. Asset- ja API-alihakemistoja ei ladattu tässä vaiheessa uudelleen. Julkinen savukoe ja health-uusintakoe tehdään ennen NCSC-käsiajoa.
- **Julkinen savukoe REL-09-vaihdon jälkeen 25.8.2026:** stagingin etusivu avautui normaalisti pakotetun päivityksen jälkeen. `GET /api/v1/health` osoitti edelleen sovelluksen tilaksi `ok` ja tietokannan tilaksi `up`. Frontend-version vaihto ei rikkonut julkista etusivua tai MariaDB-yhteyttä.
- **Ylläpidon käsiajon näkyvyys 25.8.2026:** hyväksytyn ylläpitäjän `ehdotukset.html` avautui REL-09-frontendilla ja Huijausvaroitukset-osan **Aja nyt** -toiminto näkyi. Käsiajoa ei vielä käynnistetty; ennen sitä otetaan `scam_alerts`- ja `ncsc_scrape_logs`-taulujen lähtörivimäärät.
- **NCSC-käsiajon lähtötilanne 25.8.2026:** ennen ensimmäistä REL-09-käsiajoa stagingin `scam_alerts`-taulussa oli 32 riviä ja `ncsc_scrape_logs`-taulussa 144 riviä. Määrät täsmäsivät REL-08:n hyväksyttyyn lopputilaan.
- **Ensimmäinen NCSC-käsiajo 25.8.2026:** hyväksytty ylläpitäjä käynnisti stagingin **Aja nyt** -toiminnon yhden kerran. Selainajo valmistui onnistuneesti tai hallitusti ohitettuna ilman näkyvää virhettä. Tietokantaan syntyneet rivit ja ajon tarkka tila varmistetaan seuraavaksi henkilötiedottomalla lukukyselyllä.
- **Ensimmäisen käsiajon tietokantatulos 25.8.2026:** `scam_alerts` säilyi 32 rivissä ja `ncsc_scrape_logs` kasvoi 144 rivistä 146 riviin. Kaksi uusinta lokia olivat kuluttajauutinen ja viikkokatsaus; kummankin `alerts_created` oli 0 ja viesti `recently_processed`. Lokit säilyttivät aiempien onnistuneiden ajojen rakenneversiot `news` ja `2025`. Kuuden päivän lähdekohtainen uusintaesto toimi eikä luonut varoitusduplikaatteja.
- **Välitön idempotenssikoe 25.8.2026:** ylläpidon käsiajo käynnistettiin hallitusti toisen kerran heti ensimmäisen jälkeen. `scam_alerts` pysyi edelleen 32 rivissä ja `ncsc_scrape_logs` kasvoi odotetusti 146 rivistä 148 riviin kahdella ajoa dokumentoivalla ohitusmerkinnällä. Toinen ajo ei luonut yhtään varoitusduplikaattia; lähdekohtainen uusintaesto ja ajolokitus toimivat yhdessä suunnitellusti.
- **Ylläpidon ajolokinäkymä 25.8.2026:** pakotetun sivupäivityksen jälkeen `ehdotukset.html`-sivun Kyberturvallisuuskeskuksen automaatio -osio näytti uusimmat ajot. Cloudcity-API:n ajolokireitti, frontend-provider ja ylläpidon lista toimivat stagingissä yhdessä.
- **Cloudcity-cronin luonti 25.8.2026:** stagingiin tallennettiin yksi vapaa ajastus crontab-lausekkeella `30 11,15 * * 1-5`, eli ajo on arkipäivisin klo 11.30 ja 15.30 palvelimen Europe/Helsinki-aikaa vastaavassa aikavyöhykkeessä. Komento käyttää PHP 8.4 CLI:tä ja stagingin web-juuren ulkopuolista `cron/ncsc.php`-skriptiä. Cloudcityn kertaluonteinen testaus kytkettiin päälle; testiajon tietokantatulos varmistetaan ennen cronin hyväksyntää.
- **Ensimmäisen cron-asetuksen korjaustarve 25.8.2026:** Cloudcityn listan tarkistuksessa tallennettu rivi näytti virheellisesti komennon `CMDfalse`, yhden tunnin välein toistuvan aikataulun ja tilan **Ei vielä ajettu**. `scam_alerts` säilyi 32 rivissä ja `ncsc_scrape_logs` 148 rivissä, joten virheellistä komentoa ei ollut suoritettu eikä tietokanta muuttunut. Asetusta ei hyväksytä; lomakkeen täsmälliset kentät tarkistetaan ja rivi korjataan ennen ensimmäistä ajoa.
- **Cron-asetuksen korjaus 25.8.2026:** virheellinen rivi avattiin muokkaukseen ja korjattiin Cloudcityn **Aja PHP-skripti** -toiminnoksi. PHP-skriptiksi asetettiin kotihakemistoon suhteutettu `website.aloitussivu-staging/cron/ncsc.php`, aikatauluksi vapaa `30 11,15 * * 1-5`, nimeksi stagingin NCSC-ajo ja kertaluonteinen testaus päälle. **Poista käytöstä** jätettiin pois päältä. Tallennus onnistui ja listalla näkyy nyt PHP-ajo oikealla nimellä; testisuorituksen lokitulos on vielä vahvistettava.
- **Cloudcity-cronin testisuoritus 25.8.2026:** kertaluonteisen testin jälkeen `scam_alerts` säilyi 32 rivissä ja `ncsc_scrape_logs` kasvoi 148 rivistä 150 riviin. Oikea Cloudcityn PHP-skriptiajo löysi stagingin ympäristöjuuren, latasi nykyisen yksityisen konfiguraation ja muodosti kaksi odotettua lähdekohtaista ohituslokia ilman varoitusduplikaatteja. Palvelincronin polku, PHP-ajoympäristö, MariaDB-yhteys ja idempotentti uusintaesto toimivat.
- **Käyttötilaston staging-kirjoitus 25.8.2026:** päivän lähtökysely ei vielä löytänyt `usage_daily`-riviä, koska aiemmat etusivun tarkistukset eivät olleet olleet auki 15 sekunnin lähetysviiveen yli. Etusivu pidettiin hallitusti auki vähintään 20 sekuntia, minkä jälkeen päivälle 25.8.2026 syntyi rivi, jossa oli yksi sivulataus ja yksi aiemmin syntynyt linkkiklikkaus. Saman originin frontend-provider, `POST /api/v1/usage-events`, Helsinki-päiväys ja MariaDB-aggregointi toimivat ilman käyttäjätunnisteen tallennusta.
- **REL-09-palautuskoesnapshot 25.8.2026:** alkuperäisen ennen muutoksia otetun turvallisuusvarmistuksen rinnalle vietiin nykyisestä staging-tietokannasta toinen SQL-varmistus erillisellä nimellä vain palautuskoetta varten. Vienti säilytetään repositorion ulkopuolisessa yksityisessä kansiossa eikä SQL-sisältöä kopioitu dokumentaatioon.
- **Palautuskoesnapshotin lähtörivit 25.8.2026:** henkilötiedoton 15 taulun laskenta antoi järjestyksessä `schema_migrations` 2, `admin_users` 2, `feedback_items` 3, `feedback_attachments` 0, `test_feedback_responses` 4, `link_reports` 1, `approved_links` 0, `blocked_links` 0, `scam_alerts` 32, `ncsc_scrape_logs` 150, `usage_daily` 46, `usage_page_daily` 117, `usage_link_daily` 210, `rate_limit_buckets` 31 ja `audit_log` 2 riviä. Näitä käytetään erillisen palautustietokannan täsmäytysvertailuna.
- **Erillinen REL-09-palautustietokanta 25.8.2026:** Cloudcityyn luotiin uusi `dbtpe_aloitussivu_rel09_restore` vain palautuskoetta varten. Varsinaista `dbtma_aloitussivu`-staging-tietokantaa, aiempaa REL-02-palautuskohdetta ja WordPress-tietokantaa ei tyhjennetty, korvattu tai kytketty tähän kokeeseen. Kohteen tyhjyys varmennetaan ennen SQL-tuontia.
- **Palautuskohteen tyhjyys 25.8.2026:** eksplisiittinen `information_schema.TABLES`-kysely palautti `dbtpe_aloitussivu_rel09_restore`-tietokannalle taulumäärän 0. SQL-varmistuksen tuonti voidaan tehdä tähän yksilöityyn tyhjään kohteeseen ilman olemassa olevien taulujen korvaamista.
- **SQL-palautus erilliseen tietokantaan 25.8.2026:** nykyisestä REL-09-staging-tilasta otettu `rel09-restore-test.sql` tuotiin valittuun `dbtpe_aloitussivu_rel09_restore`-tietokantaan. phpMyAdmin ilmoitti tuonnin onnistuneeksi ja suoritti 59 kyselyä. Varsinaista staging- tai WordPress-tietokantaa ei muutettu; 15 taulun rivimäärä- ja migraatiotäsmäytys tehdään seuraavaksi.
- **Palautuksen rivimäärä- ja migraatiotäsmäytys 25.8.2026:** palautustietokannan kaikki 15 taulua vastasivat snapshotin odotettuja rivimääriä (`checked_tables = 15`, `matched_tables = 15`). `schema_migrations` sisälsi sekä version `001_initial_schema` että `002_add_link_reports_triage_index`. REL-09:n varmistus ja palautus erilliseen tyhjään tietokantaan on rivitasolla valmis.
- **Palautustietokannan luku-, kirjoitus- ja peruutuskoe 25.8.2026:** erilliseen `dbtpe_aloitussivu_rel09_restore`-palautustietokantaan tehty henkilötiedoton lukukoe löysi odotetut 32 huijausvaroitusriviä. Transaktion sisällä luotiin yksi väliaikainen hyväksytty linkki ja yksi tulevaisuuteen päivätty käyttötilastorivi; molemmat näkyivät kerran transaktion aikana ja niiden määrät olivat `ROLLBACK`-komennon jälkeen 0. Keskeinen suora luku- ja kirjoituspolku toimii, eikä kokeesta jäänyt testidataa. Varsinaista staging- tai WordPress-tietokantaa ei muutettu.
- **Frontendin palautusharjoitus 25.8.2026:** REL-07:n 50 hashattua asset-tiedostoa ladattiin stagingiin rinnalle ja sen 18 ylätason tiedostoa palautettiin `public_html`-hakemistoon. Välimuistin ohittanut selainkoe avasi etusivun, lähdekoodi viittasi odotettuun REL-07:n `main-CX3NwSS3.js`-resurssiin ja health näytti sovelluksen sekä tietokannan olevan kunnossa. Tämän jälkeen REL-09:n 50 asset-tiedostoa ja 18 ylätason tiedostoa palautettiin samoihin kohteisiin. Etusivu, REL-09:n `main-C4d-59yJ.js`, health sekä hyväksytyn ylläpitäjän kirjautuminen ja ylläpitonäkymä toimivat. Aloitussivun frontend voidaan palauttaa ja palauttaa takaisin ilman tietokanta-, WordPress- tai salaisuusmuutosta.
- **WordPressin vain lukeva jälkeen-savukoe 25.8.2026:** etusivu, Etäopastus, Ajankohtaista, Yhteystiedot ja julkinen `digiopastus`-haku palauttivat HTTP 200 -vastaukset; otsikot vastasivat aiempaa vertailutasoa ja hakusivu sisälsi `Hakutulokset`-merkin. `/wp-admin/` palautti edelleen 302-ohjauksen kirjautumissivulle, kirjautumissivu HTTP 200:n ja sovittu JPEG-media HTTP 200:n, `image/jpeg`-sisältötyypin sekä aiemman 152080 tavun koon. WordPressiin ei kirjauduttu eikä sen tiedostoja, tietokantaa, teemaa, lisäosia tai asetuksia muutettu. REL-09:n palautusharjoitus ei aiheuttanut WordPress-regressiota.
- **CONTENT-P1:n rekisterinpitäjä ja yhteyskanavat 25.8.2026:** tuotevastuu hyväksyi Aloitussivun rekisterinpitäjäksi nykyistä SeniorSurf-toiminnan virallista tietosuojaselostetta vastaavan Vanhustyön keskusliitto – Centralförbundet för de gamlas väl ry:n (Y-tunnus 0215403–8, Malmin kauppatie 26, 00700 Helsinki). Tietosuojan yhteyshenkilöksi hyväksyttiin SeniorSurf-toiminnan nykyinen vastaava asiantuntija ja saavutettavuuspalautteen kanavaksi SeniorSurfin yleinen sähköpostiosoite. Yhteystiedot lisätään julkisille sivuille, mutta niitä ei toisteta tähän tekniseen päiväkirjamerkintään tarpeettomasti.
- **CONTENT-P1:n säilytysajat 25.8.2026:** tuotevastuu hyväksyi palautteiden ja linkki-ilmoitusten enimmäissäilytysajaksi 12 kuukautta, liitteiden 90 päivää, julkaisua edeltävän testipalautteen kuusi kuukautta testatun version julkaisusta tai testauksen päättymisestä ja tunnisteettomien käyttötilastokoosteiden 24 kuukautta. Tiedot poistetaan aikaisemmin, kun niitä ei enää tarvita tai soveltuva poistopyyntö sitä edellyttää.
- **Selosteiden toteutus ja tarkistus 25.8.2026:** `tietosuoja.tsx` päivitettiin kuvaamaan hyväksytty rekisterinpitäjä ja yhteyshenkilö, Cloudcityn PHP-API ja MariaDB, suojattu liitetallennus, karkea käyttötilasto, pyyntörajoituksen tietosuojamalli, ulkopuoliset rajapinnat, väliaikainen Firebase Authentication, hyväksytyt säilytysajat ja käyttäjän oikeudet. `saavutettavuus.tsx` päivitettiin hyväksytyllä palautekanavalla, 14 päivän vastausajalla sekä vuoden 2026 ajantasaisella Traficomin valvontamenettelyllä. Luonnosmerkinnät poistettiin. `npx tsc --noEmit`, `npm run build:staging`, `npm run check:secrets` ja kohdistettu `git diff --check` läpäisivät; diff-tarkistus näytti vain tunnetut LF/CRLF-varoitukset.
- **Avoimet tehtävät:** ei REL-09:n avoimia teknisiä tai CONTENT-P1-tehtäviä. Selosteiden uusi build viedään stagingiin osana REL-10:n yksilöityä julkaisukandidaattia, jotta erillistä väliversiota ei tarvitse asentaa.
- **Seuraavalle paketille luovutettu:** REL-10 saa toimivan ja palautusharjoitellun Cloudcity-stagingin, hyväksytyt säilytysajat, julkaisuvalmiit selostetekstit sekä läpäisseen staging-buildin. Tuotantoa tai WordPressiä ei muutettu REL-09:ssä.
- **Tila:** valmis ennakkoon

## Paketti: REL-10

- **Päivämäärä:** 25.8.2026 (ennakkotyö; suunniteltu pakettipäivä 28.8.2026)
- **Omistaja:** Eero Tuomenoksa / kehitys + SeniorSurf-tuotevastuu
- **Tarkistaja:** avoin
- **Riippuvuudet tarkistettu:** kyllä; REL-09 on valmis ennakkoon ja sen läpäissyt staging-build sekä palautusnäyttö on luovutettu REL-10:lle. Tuotantoa tai WordPressiä ei muuteta ennen niiden omia hyväksyttyjä tehtäviä.
- **Tehtävä 1/7 – julkisen navigaation jäädytys 25.8.2026:** etusivun Testauspalaute-linkki, automaattinen testipalautekehote, alatunnisteen Muutosloki- ja Ylläpito-linkit sekä beta-merkintä poistettiin julkisesta käyttöliittymästä. Julkista palautepainiketta, Linkkiluetteloa, tietosuojaselostetta ja saavutettavuusselostetta ei poistettu. Kehitysjonoon ei ollut etusivulla linkkiä.
- **Sisäisten sivujen säilytys:** `muutosloki.html`, `yllapito.html`, `kehitysjono.html`, `testipalaute.html` ja `testipalaute-yllapito.html` säilyvät buildissa suorilla osoitteilla. Kaikissa viidessä buildin HTML-tiedostossa on `noindex`-merkintä. Ylläpidon sisäistä navigaatiota ei muutettu.
- **Tehtävän 1 tarkistukset:** `npx tsc --noEmit`, `npm run check:secrets` ja `npm run build:staging` läpäisivät. Julkisen `index.html`-tiedoston ja sen `main`-bundlen kohdistettu tarkistus ei löytänyt sisäisiä navigointiosoitteita. `git diff --check -- App.tsx` ei löytänyt sisältövirheitä; tulosteessa oli vain tunnettu LF/CRLF-varoitus.
- **Staging-vienti:** tehtävän 1 väliversiota ei viedä erikseen palvelimelle. Kaikki REL-10-muutokset rakennetaan, yksilöidään ja viedään stagingiin yhtenä julkaisukandidaattina tehtävässä 6.
- **Uudet TEST-P2-havainnot 25.8.2026:** senioritestauksesta vastaanotettiin kolme korjattavaa käytettävyyshavaintoa. Yhden linkin alueellinen aiheryhmä avasi tarpeettoman välivaiheen, Lappeenrannan taidemuseon vanha lyhyt osoite ei toiminut ja sivuston esittely liikkui kohteiden välillä edestakaisin. Havainnot hyväksyttiin REL-10-julkaisukandidaattiin ennen sisältöjäädytystä.
- **TEST-P2-korjaukset:** yhden palvelun alueellinen aiheryhmä renderöidään nyt suoraan varsinaisena palvelulinkkinä; usean palvelun ryhmä avaa edelleen valintaikkunan. Lappeenrannan taidemuseon osoite päivitettiin kaupungin nykyiseen viralliseen museosivuun. Opastuksen vaiheet järjestettiin sivun yläosasta alaspäin siten, että mahdolliset huijausvaroitukset, suosikit ja paikallisuutiset asettuvat omille sivupaikoilleen ja viimeinen vaihe on aina Asetukset.
- **TEST-P2-tarkistukset:** `npx tsc --noEmit`, `npm run check:secrets` ja `npm run build:staging` läpäisivät. Paikallisessa Playwright-selainkokeessa Lappeenrannan yhden linkin aiheet näkyivät linkkeinä, usean linkin ryhmät painikkeina ja taidemuseolinkki osoitti uuteen viralliseen osoitteeseen. Oletusasetusten näkyvä opastus eteni kuudessa vaiheessa järjestyksessä Sää, Google-haku, Paikalliset palvelut, Kategoriat, Tee sivusta omasi ja Asetukset.
- **TEST-P2-ruutukaappausvarmistus 25.8.2026:** paikallisesta staging-buildista tallennettiin `output/playwright/rel10-yhden-linkin-aihe-suora-linkki.png`, jossa yhden palvelun aiheet näkyvät suorina korttilinkkeinä ilman linkkimäärää, sekä `output/playwright/rel10-usean-linkin-aihe-valintaikkuna.png`, jossa kahden palvelun Eläkeyhdistykset-aihe avaa edelleen valintaikkunan. Cloudcity-stagingissä näkyy REL-09-versio siihen asti, kun REL-10:n yhtenäinen julkaisukandidaatti viedään tehtävässä 6.
- **Tehtävä 2/7 – julkaistavien osioiden näkyvyys 25.8.2026:** lähdekooditarkistus vahvisti, ettei etusivu tuo nimipäivä- tai AI-komponentteja tai niiden palvelumoduuleja ajonaikaiseen käyttöön. `regionalNews`-oletus on `false`, Asetuksissa on käyttäjälle näkyvä **Näytä paikallisuutiset** -valinta ja aluepalvelupaneeli saa uutisnäkymän suoraan tästä asetuksesta.
- **Tehtävän 2 selain- ja verkkotarkistus:** puhtaassa Playwright-istunnossa Lappeenrannan paikallisuutisotsikko ei näkynyt oletuksena, RSS- tai välityspalvelupyyntöjä lähti 0 eikä AI- tai nimipäivätekstejä tai -verkkopyyntöjä havaittu. Kun **Näytä paikallisuutiset** valittiin Asetuksista, uutisotsikko tuli näkyviin ja neljä RSS- tai varapyyntöä käynnistyi; AI- ja nimipäiväpyyntöjen määrä pysyi nollassa. Nykyinen toteutus täyttää näkyvyys- ja latausrajauksen ilman uutta koodimuutosta.
- **Tehtävä 3/7 – metatiedot ja selostelinkit 25.8.2026:** Cloudcity-buildin etusivun, tietosuojasivun ja saavutettavuussivun canonical-, Open Graph-, robots- ja otsikkotiedot käyttävät lopullista `https://seniorsurf.fi/aloitus/`-osoitetta sivukohtaisine polkuineen. Sitemap sisältää vain etusivun ja kaksi julkista selostetta. Robots-tiedosto sallii julkisen sivuston, estää sisäiset ylläpito-, testi-, muutosloki-, kehitysjono- ja linkkiluettelosivut sekä viittaa lopulliseen sitemapiin. Tietosuojasivulla näkyvät hyväksytty rekisterinpitäjän nimi ja postiosoite.
- **Tehtävän 3 korjaukset ja tarkistukset:** sitemapin kolmen julkisen sivun `lastmod` päivitettiin 25.8.2026:een. Stagingin HTML-muunnos muutettiin korvaamaan robots-merkintä yhdenmukaisella `noindex, nofollow, noarchive` -arvolla aiemman ristiriitaisen kaksoismerkinnän sijaan; Cloudcity-tuotantometatiedot säilyivät ennallaan. Playwright-kokeessa etusivun Tietosuoja-linkki avasi oikean selosteen, selosteiden keskinäinen navigaatio avasi Saavutettavuusselosteen ja sivujen nimet, rekisterinpitäjän tiedot sekä takaisinlinkit näkyivät oikein. `npx tsc --noEmit`, `npm run check:secrets`, erilliset Cloudcity- ja staging-buildit sekä kohdistettu diff-tarkistus läpäisivät.
- **Tehtävän 3 nimi- ja osoitemuutos 25.8.2026:** tuotevastuu vahvisti palvelun nimeksi **Seniorin aloitussivu** ja käyttäjille viestittäväksi sekä kanoniseksi osoitteeksi `https://seniorsurf.fi/aloitus/`. Nimi päivitettiin julkiseen ylä- ja alatunnisteeseen, ohjeisiin, kaikkiin kieliversioihin, selosteisiin, 404-sivuun, manifestiin, structured dataan ja jakometatietoihin. Uusi polku päivitettiin canonical- ja Open Graph -osoitteisiin, sitemap- ja robots-tiedostoihin, julkaisun päätöslistaan, WordPress-ohjeisiin ja lanseerausteksteihin. Staging-alidomainia, Firebase-projektia, tietokantoja, PHP-nimiavaruutta tai repositorion teknistä nimeä ei muutettu.
- **Nimi- ja osoitemuutoksen varmennus:** `npx tsc --noEmit`, `npm run check:secrets` sekä erilliset Cloudcity- ja staging-buildit läpäisivät. Rakennetuissa etusivu-, tietosuoja- ja saavutettavuus-HTML-tiedostoissa nimi, canonical ja Open Graph käyttivät uusia arvoja; Cloudcity-build oli indeksoitava ja staging-buildissa oli yksi `noindex, nofollow, noarchive` -merkintä. Playwright-varmistuksessa nimi mahtui yläpalkkiin ja alatunnisteeseen, näkyvä osoite oli `seniorsurf.fi/aloitus`, ja Tietosuoja- sekä Saavutettavuusseloste avautuivat uusilla sivuotsikoilla. Ruutukaappaukset ovat `output/playwright/rel10-seniorin-aloitussivu-otsake.png` ja `output/playwright/rel10-seniorin-aloitussivu-alatunniste.png`.
- **Tehtävä 4/7 – palvelulinkkiraportti 25.8.2026:** `npm run links` tarkisti lopullisessa ajossa 2 099 yksilöllistä linkkiä. Raporttiin jäi 5 teknistä virheriviä, 231 huomiota ja 102 estolistalle kuuluvaa osoitetta. Keskeisten `constants.tsx`-palvelukategorioiden piilotettujen linkkien määrä on 0. Raportit päivitettiin tiedostoihin `docs/linkit.csv`, `docs/linkit.md`, `docs/linkit-huomiot.csv`, `docs/linkit-manuaalinen-tarkistus.csv` ja `docs/yllapito-linkkiloki.csv`; julkaistava estolista päivittyi tiedostoon `linkHealth.ts`.
- **Tehtävän 4 hyväksytyt P1/P2-korjaukset:** Teuvan vanha liikennesivu vaihdettiin kunnan nykyiseen kaikille avoimen ostoliikenteen sivuun. Vapaaehtoistyö.fi, Tanssiin.fi, Kansallisarkisto, Auranmaan Viikkolehti, Ylä-Kainuu ja Ylä-Karjala päivitettiin toimiviin osoitteisiin. Vanhat uudelleenohjausosoitteet korvattiin suorilla nykyosoitteilla muun muassa Helmetissä, Oulun seudun liikenteessä, Oulun ja Vaasan teattereissa, Pohjolan Liikenteessä, Taloussanomissa, Tiede-sivussa, Huijausinfossa, Kirkon keskusteluavussa ja Joen Severissä. Mikrobitti-linkki päivitettiin nykyiseen Bitti-palveluun ja Kuluttajaneuvonnan kaksoislinkki poistettiin.
- **Tehtävän 4 tarkistusmenetelmä ja poikkeamat:** linkkitarkistin yrittää nyt epäonnistuneen tai virheellisen HEAD-vastauksen jälkeen kevyttä GET-pyyntöä, mikä vähentää palvelinten bottisuojauksesta syntyviä vääriä hälytyksiä. Marttilan kunta, Lapin kamariorkesteri, Tampere Filharmonia, Kuluttajaneuvonta, Outlook ja Kotikokki varmennettiin selaimella toimiviksi ja kirjattiin 1.9.2026 asti voimassa oleviksi poikkeuksiksi; tekninen turvallisuusvirhe estää poikkeuslinkin edelleen. Neljä lähteetöntä tai vanhentunutta paikallislehtiosoitetta (Lakeuden Joutsen, Lakeuden Lehti, Maaselkä ja Topikki) säilyvät piilotettuina eikä niitä korvattu epävarmalla kohteella.
- **Tehtävän 4 puhelinnumerotarkistus:** linkkityökalu tarkistaa nyt näkyvän numeron ja `tel:`-linkin vastaavuuden sekä hakee saman numerosarjan palveluntarjoajan lähdesivulta. Raportissa on 81 numeroesiintymää ja 72 eri numeroa; muotovirheitä, käsin tarkistettavia rivejä ja omaan koontisivuun perustuvia rivejä on 0. Alisa Pankin vanhentunut puhelinasiakaspalvelunumero poistettiin nykyisen virallisen yhteystiedon perusteella, pankin tavallinen linkki säilytettiin ja pankkien sekä julkisten palvelujen lähdeosoitteet täsmennettiin. Kaikki 42 Kela-taksinumeroa vastaavat nykyistä Kelan lähdesivua ja ne tarkistetaan uudelleen viimeistään 1.12.2026 ennen vuoden 2027 palveluntuottajamuutosta. Tulokset ovat tiedostoissa `docs/puhelinnumerot.md` ja `docs/puhelinnumerot.csv`; tarkistus ei soita numeroihin.
- **Tehtävä 5/7 – WordPress-esittely ja tuotantopolku 25.8.2026:** julkinen selainkoe vahvisti, että `seniorsurf.fi/aloitus/` ja `seniorsurf.fi/aloitussivu-palvelu/` palauttavat vielä WordPressin 404-sivun. Tuotevastuu lukitsi käyttäjän lopulliseksi ja osoitepalkissa säilyväksi osoitteeksi `https://seniorsurf.fi/aloitus/`; aiempi 301/302-ohjaussuunnitelma tuotannon alidomainiin kumottiin. Kanoniselle `/aloitus/`-polulle ei tehdä Redirection-sääntöä eikä tuotannon alidomainia tarvita.
- **Tehtävän 5 paikallinen toteutus:** tuotannon frontend käyttää nyt `/aloitus/api/v1`-osoitetta ja API osaa poistaa konfiguroidun `/aloitus`-etuliitteen ennen sisäistä reititystä. Julkinen frontend, alihakemiston `.htaccess` ja API-entrypoint on suunniteltu kohteeseen `/website.wp33403/aloitus/`; PHP-lähdekoodi, cron, salaisuudet, lokit, välimuisti ja suojatut liitteet jäävät erilliseen `/aloitus-production/`-hakemistoon web-juuren ulkopuolelle. Stagingin nykyinen alidomain ja `/api/v1`-polku säilyvät ennallaan. Hyväksytty esittelyteksti, testit ja palautusohje ovat tiedostossa `docs/rel10-wordpress-esittely-ja-ohjaus.md`. WordPressiä tai tuotantoa ei muutettu tässä paikallisessa toteutusvaiheessa.
- **Tehtävän 5 tekninen varmennus:** tuotantopolun koepaketti muodostui onnistuneesti komennolla `npm run package:production-path`. Paketin julkinen osa käyttää `/aloitus/api/v1`-osoitetta, suhteellisia resurssipolkuja ja erillistä API-entrypointia; varsinaista asetustiedostoa, salaisuuksia tai yksityisiä hakemistoja ei ollut julkisessa osassa. Erillisessä staging-buildissa oli edelleen `/api/v1`, eikä siinä ollut `/aloitus/api/v1`-osoitetta. PHP 8.4.24 -lint läpäisi 45 tiedostoa, 42/42 API-sopimustestiä läpäisi, ja `npx tsc --noEmit`, `npm run build:staging`, `npm run check:secrets` sekä kohdistettu `git diff --check` valmistuivat ilman virheitä. Koepaketti ei ole vielä tehtävän 6 yksilöity julkaisukandidaatti eikä sitä viety palvelimelle.
- **Jatkopiste 25.8.2026:** työ keskeytettiin sovitusti seuraavaan päivään. REL-10:n tehtävä 5 odottaa enää `docs/rel10-wordpress-esittely-ja-ohjaus.md`-tiedoston esittelytekstin käyttäjähyväksyntää. Hyväksynnän jälkeen tehtävä 5 merkitään valmiiksi ja jatketaan tehtävään 6: yksilöidyn julkaisukandidaatin rakentamiseen ja saman buildin staging-vientiin. Tuotantoa tai WordPressiä ei ole vielä muutettu.
- **Avoimet tehtävät:** REL-10:n tehtävät 5–7.
- **Tila:** käynnissä ennakkoon
